---
phase: 01-ingest-profile-dq-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - src/fleet_analytics/__init__.py
  - src/fleet_analytics/config.py
  - src/fleet_analytics/ingest.py
  - tests/conftest.py
  - tests/test_rowcounts.py
  - tests/test_nulls.py
autonomous: true
requirements: [DATA-01, DATA-03]
must_haves:
  truths:
    - "Three source CSVs load into typed DuckDB Bronze tables (bronze_availability, bronze_utilization, bronze_ferry)"
    - "A fail-fast assertion confirms row counts 4,614 / 2,086 / 272,529"
    - "The 209 null AVAILABILITY_YTD values are preserved as genuine NULL (none became 0)"
    - "A test asserts null count == 209 and non-null count == 4,405"
  artifacts:
    - path: "src/fleet_analytics/config.py"
      provides: "Exact source filenames (incl. trailing space), expected row counts, explicit column-type maps"
      contains: "City Vehicle Availability .csv"
    - path: "src/fleet_analytics/ingest.py"
      provides: "DuckDB read_csv ingest into Bronze tables with explicit types + fail-fast row-count assert"
      exports: ["ingest_bronze"]
    - path: "tests/conftest.py"
      provides: "Session fixture: connect DuckDB, ingest Bronze once per session"
    - path: "tests/test_rowcounts.py"
      provides: "DATA-01 row-count assertions (4614 / 2086 / 272529)"
    - path: "tests/test_nulls.py"
      provides: "DATA-03 null preservation assertions (209 / 4405 / no-null-became-0)"
  key_links:
    - from: "src/fleet_analytics/ingest.py"
      to: "City Vehicle Availability .csv"
      via: "DuckDB read_csv with columns= type map"
      pattern: "read_csv"
    - from: "tests/conftest.py"
      to: "src/fleet_analytics/ingest.py"
      via: "session fixture imports and runs ingest_bronze"
      pattern: "ingest_bronze"
---

<objective>
Establish the Phase 1 test infrastructure and Bronze ingest. Provision a uv-managed Python 3.12 project, install the verified stack, write the explicit-type DuckDB ingest that lands the three CSVs into Bronze tables, and create the test scaffolding (conftest + row-count + null-preservation tests) that all later plans run against. This is Wave 0 + the DATA-01/DATA-03 contracts.

Purpose: All downstream work depends on the data shape being verified, not assumed. The 209-null rule (DATA-03) is won or lost here — typing AVAILABILITY_YTD as DOUBLE so blanks become genuine SQL NULL, never 0.

Output: Reproducible uv project, ingest.py, config.py, conftest.py, test_rowcounts.py, test_nulls.py — all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ingest-profile-dq-baseline/01-RESEARCH.md
@.planning/phases/01-ingest-profile-dq-baseline/01-VALIDATION.md

<verified_data_facts>
<!-- All [VERIFIED: DuckDB read of source CSV, 2026-06-02]. Hard-code these as assertion targets. -->
Source filenames (EXACT — note trailing space before .csv on availability):
  - "City Vehicle Availability .csv"
  - "Light duty city vehicle utilization data.csv"
  - "Toronto Island Ferry Ticket Counts.csv"

Row counts (data rows, excl. header):
  - bronze_availability  == 4,614
  - bronze_utilization   == 2,086
  - bronze_ferry         == 272,529

AVAILABILITY_YTD: total 4,614 / non-null 4,405 / null 209 / min 0.0 / max 1.0
UNIT_NO: VARCHAR, zero-padded (e.g. "001052") — keep VARCHAR in Bronze (integer normalization is Phase 2).
Ferry Timestamp: TIMESTAMP, tz-naive, range 2015-05-01 13:30 → 2026-06-01 22:30.
</verified_data_facts>

<interfaces>
<!-- Contracts later plans (01-02 schemas, 01-03 profile) consume. Define these here. -->
src/fleet_analytics/config.py exports:
  - AVAIL_CSV, UTIL_CSV, FERRY_CSV  (str filenames, with trailing space on AVAIL_CSV)
  - EXPECTED_ROWS: dict[str,int] = {"bronze_availability":4614,"bronze_utilization":2086,"bronze_ferry":272529}
  - AVAIL_TYPES, UTIL_TYPES, FERRY_TYPES  (dict[str,str] DuckDB column-type maps)

src/fleet_analytics/ingest.py exports:
  - ingest_bronze(con) -> duckdb connection  # creates the 3 Bronze tables, runs fail-fast row-count assert
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provision uv project, pin Python 3.12, install verified stack</name>
  <files>pyproject.toml, .python-version</files>
  <read_first>
    - .planning/phases/01-ingest-profile-dq-baseline/01-RESEARCH.md (## Standard Stack, ## Package Legitimacy Audit, ## Environment Availability)
  </read_first>
  <action>
    Initialize a uv project at the repo root if pyproject.toml is absent (`uv init --bare` or `uv init`, keeping the existing repo files). Pin Python 3.12 with `uv python pin 3.12` — uv provisions its own interpreter, so the system 3.9.13 is irrelevant. Add the runtime deps with `uv add "duckdb>=1.4,<1.5" "pandas>=2.2" "pyarrow>=16"` and the dev deps with `uv add --dev pytest "pandera>=0.26"` (these exact pins are per RESEARCH version-drift note: DuckDB 1.5.x and Pandera <0.26 are NOT on PyPI for this env). Do NOT add ydata-profiling — profiling-library HTML is optional and DATA-02 uses DuckDB SUMMARIZE (handled in 01-03). All 9 packages passed slopcheck [OK] per the Package Legitimacy Audit; none are [ASSUMED]/[SUS]/[SLOP], so no legitimacy checkpoint is required. Add a `[tool.pytest.ini_options]` section to pyproject.toml setting `testpaths = ["tests"]`.
  </action>
  <verify>
    <automated>uv run python -c "import duckdb, pandas, pyarrow, pandera; print(duckdb.__version__)"</automated>
  </verify>
  <done>pyproject.toml lists duckdb/pandas/pyarrow as deps and pytest/pandera as dev-deps; .python-version reads 3.12; uv.lock exists; the import check prints a 1.4.x DuckDB version.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write config + explicit-type Bronze ingest with fail-fast row-count assert (DATA-01)</name>
  <files>src/fleet_analytics/__init__.py, src/fleet_analytics/config.py, src/fleet_analytics/ingest.py, tests/conftest.py, tests/test_rowcounts.py</files>
  <read_first>
    - .planning/phases/01-ingest-profile-dq-baseline/01-RESEARCH.md (### Pattern 1 explicit-type CSV ingest, ### Pattern 2 fail-fast row-count assertion, ### Recommended Project Structure)
    - .planning/phases/01-ingest-profile-dq-baseline/01-VALIDATION.md (Per-Task Verification Map: DATA-01 rows)
  </read_first>
  <behavior>
    - test_availability_rowcount: bronze_availability has exactly 4,614 rows.
    - test_utilization_rowcount: bronze_utilization has exactly 2,086 rows.
    - test_ferry_rowcount: bronze_ferry has exactly 272,529 rows.
    - ingest_bronze raises AssertionError if any count mismatches (fail-fast, names the table).
    - UNIT_NO stays VARCHAR (zero-padding preserved); AVAILABILITY_YTD typed DOUBLE; ferry Timestamp typed TIMESTAMP tz-naive.
  </behavior>
  <action>
    Create config.py centralizing the three exact source filenames (with the trailing space on availability — never retype inline, per Pitfall 3), the EXPECTED_ROWS dict, and explicit DuckDB column-type maps. For availability force UNIT_NO -> VARCHAR (preserve zero-pad, per Pitfall 2) and AVAILABILITY_YTD -> DOUBLE (blank cell -> SQL NULL, NEVER 0, per Pitfall 1); YEAR -> INTEGER, IN_SERV_DT -> DATE. For utilization keep UNIT_NO -> VARCHAR and string columns for Specialized units / Utilization. For ferry type Timestamp -> TIMESTAMP (tz-naive, per Pitfall 4) and the two count columns -> BIGINT. Write ingest.py with ingest_bronze(con) that issues one CREATE TABLE ... AS SELECT * FROM read_csv(<file>, header=true, columns=<type map>) per source (DuckDB read_csv handles RFC-4180 quoting for the embedded comma in OWNER_DIVISION — do not hand-split). After creating all three tables, run the fail-fast row-count loop asserting each count against EXPECTED_ROWS, naming the table in the message. Do NOT COALESCE/fill any column. Create conftest.py with a session-scoped fixture `con` that opens an in-memory DuckDB connection and calls ingest_bronze once (per Open Question 1 recommendation: :memory: for tests). Write test_rowcounts.py with three count assertions (parametrize over EXPECTED_ROWS). Run the suite RED before implementing where practical, then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_rowcounts.py -x -q</automated>
  </verify>
  <done>ingest_bronze creates the three typed Bronze tables and fail-fast asserts counts; test_rowcounts.py passes all three assertions; UNIT_NO is VARCHAR and AVAILABILITY_YTD is DOUBLE in the resulting tables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Null-preservation guards — 209 null / 4,405 non-null / none-became-0 (DATA-03)</name>
  <files>tests/test_nulls.py</files>
  <read_first>
    - .planning/phases/01-ingest-profile-dq-baseline/01-RESEARCH.md (### The three null assertions, ## Validation Architecture: Null-preservation assertion detail)
    - .planning/phases/01-ingest-profile-dq-baseline/01-VALIDATION.md (Per-Task Verification Map: DATA-03 rows)
  </read_first>
  <behavior>
    - test_null_count: COUNT(*) - COUNT(AVAILABILITY_YTD) on bronze_availability == 209.
    - test_nonnull_count: COUNT(AVAILABILITY_YTD) == 4,405 (the denominator for every availability rate).
    - test_no_null_became_zero: non_null stays 4,405 (a fill would push it to 4,614); total == 4,614 and null + non_null == total. Optionally assert no anomalous 0.0 spike beyond the verified real zeros.
  </behavior>
  <action>
    Write test_nulls.py using the session `con` fixture. Issue the single combined query from the RESEARCH null-assertion detail selecting total, non_null (COUNT(AVAILABILITY_YTD)), and null_ct (COUNT(*) - COUNT(AVAILABILITY_YTD)) from bronze_availability. Assert total == 4,614, non_null == 4,405, null_ct == 209. The no-null-became-0 guard is satisfied structurally because the column is typed DOUBLE and never COALESCE'd/filled (per DATA-03 locked decision: exclude, never impute) — assert non_null == 4,405 to catch any accidental fill that would inflate it toward 4,614. Do not assert zero_ct == 0 (real 0.0 availability values may legitimately exist); the guard is that NULLs were not converted.
  </action>
  <verify>
    <automated>uv run pytest tests/test_nulls.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - null count == 209, non-null count == 4,405, total == 4,614.
    - No COALESCE/fillna anywhere in ingest.py (the 209 stay genuine NULL).
    - tests/test_rowcounts.py and tests/test_nulls.py both pass: `uv run pytest tests/test_rowcounts.py tests/test_nulls.py -x -q`.
  </acceptance_criteria>
  <done>All three null tests pass; non_null == 4,405 guards against any future fill regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CSV file → DuckDB ingest | Local, public, Open-Government-Licence CSVs read from the repo. No network, no auth, no user input, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | source CSV silently re-supplied/truncated | mitigate | Fail-fast row-count assert in ingest_bronze names the table and halts on mismatch (4,614 / 2,086 / 272,529). |
| T-01-SC | Tampering | uv/PyPI installs (duckdb, pandas, pyarrow, pytest, pandera) | accept | All 9 packages slopcheck [OK] and mature per RESEARCH Package Legitimacy Audit; none [ASSUMED]/[SUS]/[SLOP]. uv.lock pins exact versions. No HIGH threats. |

Honest note: this is a local read-only CSV→DuckDB ingest. Only ASVS V5 (input validation) applies, satisfied here by typed ingest + the DATA-03 guards. No injection/auth/crypto surface.
</threat_model>

<verification>
- `uv run pytest tests/test_rowcounts.py tests/test_nulls.py -x -q` is green (the quick-run command from 01-VALIDATION.md).
- `uv run python -c "import duckdb, pandas, pyarrow, pandera"` succeeds under the pinned 3.12 interpreter.
- ingest.py contains no COALESCE/fillna; AVAILABILITY_YTD is DOUBLE; UNIT_NO is VARCHAR.
</verification>

<success_criteria>
- ROADMAP success criterion 1 (row counts 4,614 / 2,086 / 272,529 with fail-fast assertion) — MET by ingest_bronze + test_rowcounts.py.
- ROADMAP success criterion 2 (209 null preserved, null==209 / non-null==4,405 asserted) — MET by typed-DOUBLE ingest + test_nulls.py.
- Wave 0 infra (uv 3.12 project, pytest + pandera installed, conftest fixture) established for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/01-ingest-profile-dq-baseline/01-01-SUMMARY.md` when done.
</output>
