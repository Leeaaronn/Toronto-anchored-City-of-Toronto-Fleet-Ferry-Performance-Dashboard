---
phase: 01-ingest-profile-dq-baseline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/fleet_analytics/schemas.py
  - tests/test_schemas.py
autonomous: true
requirements: [DATA-04, DATA-01]
must_haves:
  truths:
    - "Pandera schemas encode availability 0–1 bounds, nullability, and value sets as executable regression guards"
    - "Non-null AVAILABILITY_YTD is asserted within [0.0, 1.0]"
    - "Utilization ∈ {Underutilized, Not Underutilized} and Specialized units ∈ {Yes, No} are asserted"
    - "Explicit column dtypes (UNIT_NO VARCHAR, AVAILABILITY_YTD DOUBLE) are enforced by schema"
    - "All Pandera regression guards run green"
  artifacts:
    - path: "src/fleet_analytics/schemas.py"
      provides: "Pandera DataFrameSchemas for the three Bronze tables (the DQ contracts)"
      exports: ["availability_schema", "utilization_schema", "ferry_schema"]
    - path: "tests/test_schemas.py"
      provides: "DATA-04 Pandera validation tests (bounds, value sets, dtypes)"
  key_links:
    - from: "tests/test_schemas.py"
      to: "src/fleet_analytics/schemas.py"
      via: "validate the Bronze .df() frames against the schemas"
      pattern: "schema.validate"
    - from: "src/fleet_analytics/schemas.py"
      to: "AVAILABILITY_YTD"
      via: "Check.in_range(0.0, 1.0), nullable=True"
      pattern: "in_range"
---

<objective>
Freeze the DATA-04 data-quality contracts as executable Pandera regression guards. Encode the availability 0–1 bounds, the 209-null nullability rule, the categorical value sets, and the explicit dtypes as declarative Pandera schemas, then assert them in pytest against the Bronze frames.

Purpose: Every DQ fact must be both documented (the 01-03 DQ report) and ENFORCED (here). Pandera does both from one definition — when a future CSV re-supply breaks the contract, the test goes red.

Output: src/fleet_analytics/schemas.py + tests/test_schemas.py, green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ingest-profile-dq-baseline/01-RESEARCH.md
@.planning/phases/01-ingest-profile-dq-baseline/01-VALIDATION.md
@.planning/phases/01-ingest-profile-dq-baseline/01-01-SUMMARY.md

<interfaces>
<!-- From 01-01. Use these directly; the conftest `con` fixture is already defined. -->
tests/conftest.py provides a session-scoped `con` fixture (in-memory DuckDB with Bronze tables ingested).
Convert any Bronze table to a pandas frame for Pandera via: con.execute("SELECT * FROM bronze_availability").df()

src/fleet_analytics/config.py exports value-set facts to assert against:
  - AVAILABILITY_YTD non-null ∈ [0.0, 1.0]
  - Utilization ∈ {"Underutilized", "Not Underutilized"}
  - Specialized units ∈ {"Yes", "No"}
</interfaces>

<verified_data_facts>
<!-- [VERIFIED 2026-06-02] assertion targets -->
AVAILABILITY_YTD: non-null 4,405 / null 209; min 0.0 / max 1.0 (already in bounds — assert, never clamp).
UNIT_NO: VARCHAR (zero-padded, nullable=False).
Utilization value set: {Underutilized (120), Not Underutilized (1,966)}.
Specialized units value set: {Yes (825), No (1,261)}.
</verified_data_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pandera schemas for the three Bronze tables (DATA-04 contracts)</name>
  <files>src/fleet_analytics/schemas.py</files>
  <read_first>
    - .planning/phases/01-ingest-profile-dq-baseline/01-RESEARCH.md (### Pattern 3 Pandera DQ contract, ## Don't Hand-Roll)
    - .planning/phases/01-ingest-profile-dq-baseline/01-01-SUMMARY.md (Bronze table/column shapes as built)
  </read_first>
  <behavior>
    - availability_schema validates: UNIT_NO Column(str, nullable=False); AVAILABILITY_YTD Column(float, Check.in_range(0.0,1.0), nullable=True) — the 209 nulls are LEGAL; CATEGORY_CLASS Column(str, nullable=False).
    - utilization_schema validates: Utilization Column(str, Check.isin(["Underutilized","Not Underutilized"])); Specialized units Column(str, Check.isin(["Yes","No"])); UNIT_NO Column(str, nullable=False).
    - ferry_schema validates: Sales Count / Redemption Count as integer (nullable=False); Timestamp as datetime.
    - All schemas use strict=False (Bronze has more columns than the contracted ones).
  </behavior>
  <action>
    Create schemas.py importing `pandera.pandas as pa` and Column/Check/DataFrameSchema (per RESEARCH Pattern 3). Define availability_schema with AVAILABILITY_YTD as Column(float, Check.in_range(0.0, 1.0), nullable=True) — nullable=True is the contract that the 209 nulls are legal (DATA-03 locked decision: exclude, never impute). Define utilization_schema with Check.isin value-set guards on Utilization and Specialized units. Define ferry_schema typing the count columns and Timestamp. Use strict=False on all three so extra Bronze columns are tolerated. Do NOT hand-roll assert ladders (per Don't Hand-Roll) — the schema is the single declarative source of the contract.
  </action>
  <verify>
    <automated>uv run python -c "from fleet_analytics.schemas import availability_schema, utilization_schema, ferry_schema; print('ok')"</automated>
  </verify>
  <done>schemas.py exports the three DataFrameSchemas; AVAILABILITY_YTD carries Check.in_range(0,1) with nullable=True; value-set Checks present on the utilization categoricals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pandera validation tests — bounds, value sets, dtypes (DATA-04)</name>
  <files>tests/test_schemas.py</files>
  <read_first>
    - .planning/phases/01-ingest-profile-dq-baseline/01-VALIDATION.md (Per-Task Verification Map: DATA-04 rows; test_availability_bounds, test_value_sets)
  </read_first>
  <behavior>
    - test_availability_bounds: availability_schema.validate(bronze_availability.df()) passes; no non-null value outside [0.0, 1.0].
    - test_value_sets: utilization_schema.validate(bronze_utilization.df()) passes; Utilization ∈ {Underutilized, Not Underutilized}; Specialized units ∈ {Yes, No}.
    - test_dtypes: UNIT_NO is object/str and AVAILABILITY_YTD is float in the validated availability frame (asserts the explicit-type ingest held).
    - ferry_schema.validate(bronze_ferry.df()) passes.
  </behavior>
  <action>
    Write test_schemas.py using the session `con` fixture from conftest. For each table, pull the Bronze frame via con.execute("SELECT * FROM <table>").df() and call the matching schema's .validate(frame) inside tests named per the VALIDATION map (test_availability_bounds, test_value_sets, plus a dtype assertion). A passing .validate() is the green signal; a contract violation raises SchemaError and fails the test. Keep the named test functions matching the 01-VALIDATION Per-Task Verification Map so the per-req test map stays aligned.
  </action>
  <verify>
    <automated>uv run pytest tests/test_schemas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - test_availability_bounds passes: all non-null AVAILABILITY_YTD within [0.0, 1.0].
    - test_value_sets passes: Utilization ∈ {Underutilized, Not Underutilized}, Specialized units ∈ {Yes, No}.
    - dtype assertion confirms UNIT_NO str + AVAILABILITY_YTD float.
    - Full suite green so far: `uv run pytest -q`.
  </acceptance_criteria>
  <done>tests/test_schemas.py passes; the DATA-04 contracts (bounds, value sets, dtypes) run as executable regression guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bronze frame → Pandera validation | In-process pandas frames derived from local Bronze tables. No external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | malformed/out-of-range data entering downstream KPI math | mitigate | Pandera schemas (the only ASVS V5 input-validation control in this phase) reject out-of-bounds availability and unknown categorical values; run as pytest regression guards. |

Honest note: no auth/session/crypto/network surface. The single applicable control is input validation (ASVS V5), which is exactly what these schemas provide. No HIGH threats.
</threat_model>

<verification>
- `uv run pytest tests/test_schemas.py -x -q` is green.
- schemas.py uses Pandera (no hand-rolled assert ladders); AVAILABILITY_YTD bound is Check.in_range(0.0, 1.0) with nullable=True.
</verification>

<success_criteria>
- ROADMAP success criterion 4 (Pandera schemas encode row-count, 209-null, 4,405-non-null, and 0–1 bounds as green regression guards) — MET (bounds + value-set + dtype guards here; the 209-null / 4,405 / row-count assertions live in 01-01's test_nulls/test_rowcounts, completing the set).
</success_criteria>

<output>
Create `.planning/phases/01-ingest-profile-dq-baseline/01-02-SUMMARY.md` when done.
</output>
