---
phase: 01-ingest-profile-dq-baseline
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/fleet_analytics/profile.py
  - tests/test_profile.py
  - deliverables/data_dictionary.md
  - deliverables/dq_report.md
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "profile.py emits a deterministic dict of DQ facts from DuckDB SUMMARIZE + targeted SQL"
    - "A data dictionary documents every Bronze column (name, type, nullability, notes)"
    - "A DQ report documents nulls, ranges, ferry skew (median 12 / max 7,229), the retired-dataset pull-date caveat, and the 5.8% vs 14% underutilization discrepancy as a stated insight"
    - "Headline numbers are testable: ferry Sales median 12 / max 7,229; underutilized 5.8% (120/2,086); 21 divisions; YEAR 1982–2026"
  artifacts:
    - path: "src/fleet_analytics/profile.py"
      provides: "SUMMARIZE + targeted DQ queries returning a dict of facts"
      exports: ["profile_facts"]
    - path: "deliverables/dq_report.md"
      provides: "DQ report with nulls, ranges, skew, pull-date caveat, 5.8% vs 14% stated insight"
      contains: "5.8%"
    - path: "deliverables/data_dictionary.md"
      provides: "Per-column data dictionary for the three Bronze tables"
      contains: "AVAILABILITY_YTD"
    - path: "tests/test_profile.py"
      provides: "DATA-02 assertions on profile_facts output + DQ-report artifact-exists check"
  key_links:
    - from: "tests/test_profile.py"
      to: "src/fleet_analytics/profile.py"
      via: "assert on profile_facts() return dict"
      pattern: "profile_facts"
    - from: "src/fleet_analytics/profile.py"
      to: "bronze_ferry / bronze_availability / bronze_utilization"
      via: "DuckDB SUMMARIZE + targeted MEDIAN/MAX/COUNT DISTINCT queries"
      pattern: "SUMMARIZE|median|MAX"
---

<objective>
Produce the DATA-02 deliverables: a deterministic profiling function, a data dictionary, and a DQ report. All numbers come from DuckDB SUMMARIZE + targeted SQL (not a profiling-library HTML — that path is optional and skipped per RESEARCH). The DQ report frames the documented caveats and the 5.8% vs 14% underutilization gap as stated, cited insights.

Purpose: Every data-quality fact must be documented as a stated assumption BEFORE any transform is written (the Phase 1 goal). The 5.8% vs 14% gap is framed as a period/right-sizing insight, not an error.

Output: profile.py, test_profile.py, deliverables/data_dictionary.md, deliverables/dq_report.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-ingest-profile-dq-baseline/01-RESEARCH.md
@.planning/phases/01-ingest-profile-dq-baseline/01-VALIDATION.md
@.planning/phases/01-ingest-profile-dq-baseline/01-01-SUMMARY.md

<interfaces>
<!-- From 01-01. The conftest `con` fixture (in-memory DuckDB with Bronze tables) is reused. -->
tests/conftest.py provides session-scoped `con` fixture.
profile.py should expose: profile_facts(con) -> dict  # deterministic DQ facts for the report and tests.
</interfaces>

<verified_data_facts>
<!-- [VERIFIED 2026-06-02] — these are the exact targets the DQ report and tests must carry. -->
Row counts: 4,614 / 2,086 / 272,529.
AVAILABILITY_YTD: non-null 4,405 / null 209 (4.53% null); min 0.0 / max 1.0.
Ferry Sales Count: median 12 / max 7,229. Redemption Count: median 11 / max 7,216. Heavy right skew — NOT an error (peak windows).
Underutilized: 120 / 2,086 = 5.75% ≈ 5.8% (contrasts with the AG audit's ~14%).
Distinct OWNER_DIVISION: 21. Distinct CATEGORY_CLASS: 19. Distinct STATUS_DESC: 4.
YEAR range: 1982 → 2026. Ferry Timestamp range: 2015-05-01 → 2026-06-01 (all 12 years present).

Narrative caveats to CITE (not compute) — A1/A2 from Assumptions Log:
  - A1: availability is a retired/point-in-time snapshot pull, not live → record pull/snapshot date.
  - A2: the ~14% audit underutilization benchmark is from the AG 2019.AU2.3 Operational Review, not derivable from the CSV → inline citation.
Sources to cite: City of Toronto Open Data portal; May 2023 FSD General Government Committee report; AG Operational Review 2019.AU2.2 / 2019.AU2.3. Licence: Open Government Licence – Toronto.
</verified_data_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: profile.py — deterministic DQ facts via SUMMARIZE + targeted SQL, with tests (DATA-02)</name>
  <files>src/fleet_analytics/profile.py, tests/test_profile.py</files>
  <read_first>
    - .planning/phases/01-ingest-profile-dq-baseline/01-RESEARCH.md (### DuckDB SUMMARIZE for the DQ report, ## Validation Architecture: DQ-report / profiling observable outputs)
    - .planning/phases/01-ingest-profile-dq-baseline/01-VALIDATION.md (Per-Task Verification Map: DATA-02 row)
  </read_first>
  <behavior>
    - profile_facts(con) returns a dict carrying: per-table row counts (4614/2086/272529); AVAILABILITY_YTD null_count (209), non_null (4405), min (0.0), max (1.0); ferry Sales median (12) and max (7229); Redemption median (11) / max (7216); underutilized_count (120), underutilized_rate (≈0.058); distinct OWNER_DIVISION (21), CATEGORY_CLASS (19); YEAR min (1982) / max (2026).
    - test_profile.py asserts: ferry Sales median == 12, Sales max == 7229, underutilized rate rounds to 5.8% (round(rate*100,1)==5.8), distinct OWNER_DIVISION == 21, YEAR range == (1982, 2026).
  </behavior>
  <action>
    Create profile.py with profile_facts(con) using DuckDB SUMMARIZE plus targeted queries (MEDIAN, MAX, COUNT, COUNT(DISTINCT), MIN/MAX on YEAR) per RESEARCH — deterministic and citable, do not hand-roll aggregation loops (Don't Hand-Roll). Compute underutilized_rate as COUNT(*) FILTER (WHERE Utilization='Underutilized') / COUNT(*) on bronze_utilization (120/2,086). Return a flat dict keyed for direct assertion. Write test_profile.py using the `con` fixture asserting the exact headline numbers from the VALIDATION DATA-02 row (ferry Sales median 12 / max 7,229; underutilized 5.8%; 21 divisions; YEAR 1982–2026). Treat the ferry max 7,229 as real skew, not an error (Anti-Patterns). Use exact integer equality for counts and median; round the rate to 1 decimal for the 5.8% assertion.
  </action>
  <verify>
    <automated>uv run pytest tests/test_profile.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - profile_facts returns ferry Sales median == 12 and max == 7,229.
    - underutilized rate rounds to 5.8% (120/2,086).
    - distinct OWNER_DIVISION == 21; YEAR range == (1982, 2026).
    - tests/test_profile.py passes.
  </acceptance_criteria>
  <done>profile_facts emits all DATA-02 headline numbers deterministically from SQL; test_profile.py is green.</done>
</task>

<task type="auto">
  <name>Task 2: Author data_dictionary.md and dq_report.md from the profiled facts</name>
  <files>deliverables/data_dictionary.md, deliverables/dq_report.md</files>
  <read_first>
    - .planning/phases/01-ingest-profile-dq-baseline/01-RESEARCH.md (## Verified Data Facts, ## Assumptions Log A1/A2, <user_constraints> Sourcing)
    - .planning/phases/01-ingest-profile-dq-baseline/01-VALIDATION.md (## Manual-Only Verifications)
    - src/fleet_analytics/profile.py (the facts to transcribe)
  </read_first>
  <action>
    Write data_dictionary.md documenting every Bronze column for all three tables: column name, DuckDB type as ingested (UNIT_NO VARCHAR, AVAILABILITY_YTD DOUBLE, ferry Timestamp TIMESTAMP, etc.), nullability, and notes (zero-padding on UNIT_NO; the 209-null AVAILABILITY_YTD; value sets for Utilization / Specialized units). Write dq_report.md transcribing the profile_facts numbers and covering, at minimum: per-table row counts; the 209-null AVAILABILITY_YTD gap (excluded, never imputed — denominator 4,405); availability range [0,1]; the ferry skew (Sales median 12 vs max 7,229) explicitly framed as a real peak-window outlier story, NOT an error; the retired-dataset pull-date caveat (A1, cite the snapshot/pull date and that the availability dataset is portal-retired); and the 5.8% vs 14% underutilization discrepancy framed as a STATED INSIGHT (period/right-sizing framing) with an inline citation to the AG 2019.AU2.3 Operational Review (A2). Cite the three primary sources (City of Toronto Open Data portal, May 2023 FSD report, AG 2019.AU2.2/2.3) and the Open Government Licence – Toronto. Do NOT present the ~14% benchmark or pull-date as computed — they are cited narrative facts.
  </action>
  <verify>
    <automated>uv run pytest tests/test_profile.py -x -q</automated>
    <human-check>Read deliverables/dq_report.md: confirm the 5.8% vs 14% paragraph cites the AG audit and frames the gap as a period/right-sizing insight (not an error), and that the retired-dataset pull-date caveat with a recorded snapshot date is present (per 01-VALIDATION Manual-Only Verifications).</human-check>
  </verify>
  <done>data_dictionary.md covers all Bronze columns with types/nullability; dq_report.md documents nulls, ranges, ferry skew (12/7,229), the pull-date caveat, and the 5.8% vs 14% gap as a cited stated insight; all three primary sources and the licence are cited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bronze tables → profiling SQL → markdown deliverables | In-process read-only aggregation of local Bronze tables into documentation. No external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information disclosure | uncited external benchmark (~14%) presented as derived | mitigate | DQ report must cite the AG audit for the ~14% figure and the pull-date caveat (A1/A2); profiled numbers come from deterministic SQL. Prevents an indefensible/unsourced claim in an audit-graded deliverable. |

Honest note: local read-only CSVs, public Open-Government-Licence data, no secrets/auth/network. No HIGH threats; the only real risk is documentation provenance, mitigated by mandatory citations.
</threat_model>

<verification>
- `uv run pytest tests/test_profile.py -x -q` is green (DATA-02 headline numbers).
- deliverables/data_dictionary.md and deliverables/dq_report.md exist and carry the verified numbers and required caveats.
- Full phase suite green: `uv run pytest -q` (the hard gate that unlocks Phase 2).
</verification>

<success_criteria>
- ROADMAP success criterion 3 (data dictionary + DQ report documenting nulls, ranges, ferry skew median 12 / max 7,229, retired-dataset pull-date caveat, and the 5.8% vs 14% discrepancy as a stated insight) — MET.
</success_criteria>

<output>
Create `.planning/phases/01-ingest-profile-dq-baseline/01-03-SUMMARY.md` when done.
</output>
