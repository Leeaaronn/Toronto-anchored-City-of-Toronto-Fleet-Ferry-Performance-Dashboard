---
phase: 03-kpi-layer-measures-spec
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/test_kpis.py
  - tests/conftest.py
autonomous: true
requirements:
  - KPI-01

must_haves:
  truths:
    - "The canonical pooled-per-vehicle-mean grand-total guard asserts the rate is in [0,1], equals the snapshot value, and is NOT the mean-of-class-means"
    - "A guard asserts 2020 ferry volume < 2019 ferry volume (COVID dip present), within the complete-years range"
    - "Distribution sanity guards assert ferry Sales max == 7229 and median ≈ 12"
    - "A 209-NULL-exclusion guard asserts the availability denominator is 4,405 (COUNT(*) - COUNT(AVAILABILITY_YTD) == 209)"
    - "The committed snapshot values ARE the regression contract: tests assert the live compute matches the persisted kpi_values.json / CSV values"
  artifacts:
    - path: "tests/test_kpis.py"
      provides: "pytest guards asserting the KPI snapshot values (snapshot-as-contract)"
      min_lines: 70
      contains: "def test_"
  key_links:
    - from: "tests/test_kpis.py"
      to: "tests/conftest.py gold fixture"
      via: "reuse session-scoped Gold fixture (extended to run kpis.build_all)"
      pattern: "def test_.*\\(gold"
    - from: "tests/test_kpis.py"
      to: "data/kpi/kpi_values.json"
      via: "load snapshot and assert live compute matches"
      pattern: "kpi_values.json"
---

<objective>
Lock the Phase 3 KPIs with pytest guards that treat the committed snapshot
(`data/kpi/kpi_values.json` + per-table CSVs) as the regression contract (D-06).
Every headline KPI value the dashboard must reproduce is asserted here against the
live compute and the persisted snapshot, including the canonical pooled-mean
grand-total guard, the 2020<2019 COVID-dip guard, the 7229/12 distribution sanity
checks, and the 209-NULL-exclusion (denominator 4,405) guard.

Purpose: A green `tests/test_kpis.py` is the evidence that the KPI layer is correct
and the snapshot is trustworthy — the proof carried into Phase 4 and the panel interview.
Output: `tests/test_kpis.py` (and a small `gold`-chain extension in conftest.py).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-kpi-layer-measures-spec/03-CONTEXT.md
@.planning/phases/03-kpi-layer-measures-spec/03-PATTERNS.md

<interfaces>
<!-- Snapshot keys produced by Plan 01 (assert against these). -->
data/kpi/kpi_values.json (flat-keyed): overall_availability_rate (~0.8899),
  availability_nonnull_n (4405), availability_null_n (209),
  overall_underutilization_rate (~0.0572), ferry_sales_max (7229),
  ferry_sales_median (12), plus by-class rate/target/gap and 2019/2020 ferry totals.
data/kpi/ferry_yoy.csv: rows ONLY for years 2016..2025.
Verified ground truth: 2019 Sales=1,249,725 ; 2020 Sales=366,606 (2020 < 2019).
</interfaces>

@tests/conftest.py
@tests/test_join_integrity.py
@tests/test_derived_fields.py
@src/fleet_analytics/kpis.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the gold fixture so KPIs build once per session</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (the file being modified — lines 22-35: the `gold` fixture depends on `con`, runs `transform.build_all(con)` then `model.build_all(con)` and returns con; this is the exact chain to extend)
    - src/fleet_analytics/kpis.py (the build_all signature added in Plan 01)
    - .planning/phases/03-kpi-layer-measures-spec/03-PATTERNS.md (the test fixture-reuse note lines 123-125)
  </read_first>
  <action>
    Add a single `kpis.build_all(con)` call to the existing `gold` fixture chain in conftest.py, after `model.build_all(con)`, importing kpis alongside the existing local `from fleet_analytics import model, transform` import (extend it to `model, transform, kpis` or add the kpis import). Do NOT change fixture scope or signature — this mirrors how Plan 02 of Phase 2 added the `model.build_all(con)` line. The fixture must still return `con`. No new fixture is required unless the snapshot-file assertions need one; if so, add a tiny session-scoped helper that loads `data/kpi/kpi_values.json` once.
  </action>
  <verify>
    <automated>uv run python -c "import inspect, tests.conftest as c; src=inspect.getsource(c.gold.__wrapped__ if hasattr(c.gold,'__wrapped__') else c.gold); assert 'kpis' in src; print('fixture chain OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The gold fixture runs kpis.build_all(con) after model.build_all(con) and still returns con.
    - Fixture scope remains session; no duplicate Bronze ingest.
  </acceptance_criteria>
  <done>conftest.py gold fixture builds the KPI layer once per session; the verify confirms 'kpis' is in the fixture chain.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/test_kpis.py snapshot-as-contract guards</name>
  <files>tests/test_kpis.py</files>
  <read_first>
    - tests/test_join_integrity.py (lines 17-26 — the `_scalar(con, sql)` helper + `def test_x(gold): value=_scalar(...); assert value == EXPECTED` exact-value idiom to copy)
    - tests/test_derived_fields.py (lines 64-69 — the 209-NULL count guard `SELECT COUNT(*) - COUNT(AVAILABILITY_YTD)`; lines 72-110 — `import pytest # noqa: E402` local convention + `@pytest.mark.parametrize(..., ids=[...])` table-driven style for the by-class cases)
    - tests/conftest.py (the gold fixture extended in Task 1)
    - src/fleet_analytics/kpis.py (the builders + build_scalars feeding the assertions)
    - data/kpi/kpi_values.json + the 7 CSVs (the committed snapshot these tests assert against — the regression contract, D-06)
    - .planning/phases/03-kpi-layer-measures-spec/03-PATTERNS.md (the test section lines 121-149 — scalar-guard, parametrized, 209-NULL, snapshot-as-contract idioms)
  </read_first>
  <action>
    Create tests/test_kpis.py mirroring test_join_integrity.py: module docstring naming the D-06 guards, a `_scalar(con, sql)` helper, and `def test_x(gold)` functions. Write these guards: (1) `test_pooled_grand_total_mean` — compute the pooled per-vehicle mean `AVG(AVAILABILITY_YTD) FROM fact_vehicle`, assert it is in [0,1], assert it rounds to the snapshot `overall_availability_rate` (~0.8899), AND assert it differs from the mean-of-class-means (compute AVG over per-UNIT_TYPE AVGs and assert the two are NOT equal — this is the canonical "pooled, not average-of-averages" proof). (2) `test_availability_null_exclusion` — assert `COUNT(*) - COUNT(AVAILABILITY_YTD) == 209` and `COUNT(AVAILABILITY_YTD) == 4405` (the denominator). (3) `test_ferry_2020_below_2019` — sum Sales for 2019 and 2020, `assert vol_2020 < vol_2019`, scoped within the complete-years range. (4) `test_ferry_distribution_sanity` — `assert sales_max == 7229` and `assert median ≈ 12` (use ==12 or abs tolerance). (5) Parametrized `test_availability_by_class` over the 5 classes asserting each class's computed rate/gap is in sane bounds and gap == rate - target (D-03 signed). (6) `test_snapshot_matches_live` — load `data/kpi/kpi_values.json`, assert its `overall_availability_rate`, `availability_null_n`, `ferry_sales_max` match the live `gold`-fixture compute (snapshot-as-contract, the test_export.py re-read pattern). (7) `test_ferry_yoy_complete_years_only` — read `data/kpi/ferry_yoy.csv` (or the live builder) and assert its year set ⊆ {2016..2025} (no 2015/2026 in the YoY rows). Place `import pytest # noqa: E402` near the parametrized test per the local convention. No fenced implementation beyond these test signatures; copy idioms from the read_first analogs.
  </action>
  <verify>
    <automated>uv run pytest tests/test_kpis.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_kpis.py` exits 0 with all guards passing.
    - test_pooled_grand_total_mean asserts the rate is in [0,1], ≈0.8899, AND not equal to the mean-of-class-means.
    - test_availability_null_exclusion asserts null count == 209 and non-null denominator == 4405.
    - test_ferry_2020_below_2019 asserts 2020 Sales < 2019 Sales.
    - test_ferry_distribution_sanity asserts max == 7229 and median ≈ 12.
    - test_snapshot_matches_live asserts the persisted kpi_values.json equals the live compute for the headline keys.
    - test_ferry_yoy_complete_years_only asserts the YoY year set is within 2016..2025.
  </acceptance_criteria>
  <done>tests/test_kpis.py exists with the 7 guard groups above and `uv run pytest tests/test_kpis.py -q` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/gold + data/kpi → tests | Internal committed artifacts read by the test suite. No external input, no network, no auth. |

This plan only adds read-only test guards over internal committed artifacts. No
network surface, no untrusted input, no secrets. Threat surface is minimal.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | snapshot drift vs live compute | mitigate | `test_snapshot_matches_live` asserts the committed kpi_values.json equals the live compute for headline keys, so a silently-edited snapshot fails CI (the D-06 contract). |
| T-03-05 | Information Disclosure | test fixtures | accept | Tests read public-data aggregates only; no PII/secrets. Low-value. |
| T-03-06 | Tampering (supply chain) | python deps | accept | No new package installs (pytest already vetted/pinned in Phase 1). No legitimacy gate required. |
</threat_model>

<verification>
- `uv run pytest tests/test_kpis.py -q` exits 0.
- `uv run pytest -q` (full suite) stays green — the extended gold fixture does not break existing tests.
</verification>

<success_criteria>
- The canonical pooled-mean, 2020<2019, 7229/12, and 209-NULL guards all pass.
- The committed snapshot is proven to equal the live compute (snapshot-as-contract).
</success_criteria>

<output>
Create `.planning/phases/03-kpi-layer-measures-spec/03-02-SUMMARY.md` when done.
</output>
