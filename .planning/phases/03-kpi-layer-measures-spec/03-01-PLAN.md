---
phase: 03-kpi-layer-measures-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fleet_analytics/config.py
  - src/fleet_analytics/kpis.py
  - data/kpi/kpi_values.json
  - data/kpi/availability_by_class.csv
  - data/kpi/availability_by_division.csv
  - data/kpi/exception_list.csv
  - data/kpi/underutilization_by_division.csv
  - data/kpi/ferry_yoy.csv
  - data/kpi/ferry_heatmap.csv
  - data/kpi/sales_redemption_gap.csv
autonomous: true
requirements:
  - KPI-01

must_haves:
  truths:
    - "Overall availability is computed as the pooled per-vehicle mean AVG(AVAILABILITY_YTD) over the 4,405 non-null rows, in [0,1], NOT the mean-of-class-means"
    - "Availability by asset class vs target with signed gap-to-target (actual - target) is computed for Light/Medium/Heavy/Off-Road/Other"
    - "The exception list ranks below-class-target units; the disposal-candidate screen = below-class-target AND Utilization='Underutilized'"
    - "Underutilization rate overall (~5.7%) and by using division is computed on the matched light-duty subset"
    - "Ferry lifetime/period totals use ALL data; YoY is computed only on complete years 2016-2025; seasonality pools all years by month; DoW x hour heatmap and signed sales-redemption gap are computed"
    - "A committed snapshot (kpi_values.json scalars + per-table CSVs) is written to data/kpi/ and is reviewable in git"
  artifacts:
    - path: "src/fleet_analytics/kpis.py"
      provides: "DuckDB SQL-first KPI compute module: per-KPI builders + build_all(con) orchestrator + write_kpi_snapshot(con)"
      min_lines: 120
      contains: "def build_all"
    - path: "src/fleet_analytics/config.py"
      provides: "ASSET_CLASS_TARGETS, UNIT_TYPE->class-label mapping, KPI_DIR, KPI_TABLE_CSVS constants"
      contains: "ASSET_CLASS_TARGETS"
    - path: "data/kpi/kpi_values.json"
      provides: "Authoritative scalar + benchmark snapshot (flat-keyed for direct assertion)"
      contains: "overall_availability_rate"
    - path: "data/kpi/ferry_yoy.csv"
      provides: "Per-complete-year ferry totals (2016-2025) for the 2020<2019 COVID-dip guard"
  key_links:
    - from: "src/fleet_analytics/kpis.py"
      to: "config.ASSET_CLASS_TARGETS"
      via: "? / join bind of class targets into availability-by-class SQL"
      pattern: "ASSET_CLASS_TARGETS"
    - from: "src/fleet_analytics/kpis.py"
      to: "data/gold/*.parquet"
      via: "read_parquet over config.GOLD_DIR"
      pattern: "read_parquet"
    - from: "src/fleet_analytics/kpis.py"
      to: "data/kpi/kpi_values.json"
      via: "json.dump over a profile.py-style flat dict"
      pattern: "json.dump"
---

<objective>
Build the authoritative KPI compute layer for Phase 3: a `kpis.py` DuckDB SQL-first
module that reads the Phase 2 Gold Parquet and computes every Domain A (fleet
maintenance) and Domain B (ferry) KPI cross-checked against audit benchmarks, then
persists the ground-truth values as a committed snapshot (scalars JSON + per-table
CSVs). This is the source of truth that the test guards (Plan 02) and the two
deliverable docs (Plan 03) both consume.

Purpose: Every number the Power BI dashboard later shows must reproduce a value in
this snapshot — the snapshot makes the dashboard falsifiable.
Output: `config.py` KPI constants, `src/fleet_analytics/kpis.py`, and the
`data/kpi/` snapshot (1 JSON + 7 CSVs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-kpi-layer-measures-spec/03-CONTEXT.md
@.planning/phases/03-kpi-layer-measures-spec/03-PATTERNS.md

<interfaces>
<!-- Gold Parquet column contract (verified live against data/gold/*.parquet). -->
<!-- Executor must reference ONLY these confirmed names — no codebase exploration needed. -->

fact_vehicle (4,614 rows; AVAILABILITY_YTD carries 209 genuine NULLs):
  _id, UNIT_NO, YEAR, MAKE, MODEL, CATEGORY, CAT_DESC, UNIT_TYPE, CATEGORY_CLASS,
  CAT_GRP, IN_SERV_DT, STATUS_DESC, CLASS2, HIGH_PRIORITY, OWNER_DIVISION,
  REF_DIVISION, SEASONAL, AVAILABILITY_YTD, unit_key_int, fleet_age, Utilization,
  specialized_units, REF_USING_DIV, owner_division_key, using_division_key

  - ASSET CLASS lives in column `UNIT_TYPE` with EXACTLY these 5 values:
    'LIGHT DUTY', 'MEDIUM DUTY', 'HEAVY DUTY', 'OFF-ROAD', 'OTHER'
    These map to the CONTEXT target labels Light/Medium/Heavy/Off-Road/Other.
  - `Utilization` is NULL for non-light-duty (~2,528 rows); value set on matched
    rows is {'Underutilized', 'Not Underutilized'}.
  - `specialized_units` (the Specialized split) value set is {'Yes', 'No'} on matched rows.
  - `using_division_key` is the FK for the "using division" rollup; resolve names via dim_division.

fact_ferry (272,529 rows):
  _id, Timestamp, "Redemption Count", "Sales Count", ts_15, season, daypart,
  day_of_week, is_weekend, sales_redemption_gap
  - year(Timestamp) spans 2015..2026; 2015 (~8mo) and 2026 (~6mo) are PARTIAL years.
  - hour-of-day for the heatmap = hour(Timestamp) or via dim_time; day-of-week = day_of_week.

dim_division: division_key, division_name
dim_date: date_key, year, month, month_name, day, day_of_week, is_weekend, quarter
dim_time: time_key, time_of_day, hour, minute

Verified ground-truth values (use as the snapshot/assert targets):
  overall pooled availability rate = 0.8899126467628139
  non-null / null AVAILABILITY_YTD = 4405 / 209
  overall underutilization rate (matched) = 0.05721153846153846 (~5.7%, framed "5.8%")
  ferry Sales Count max = 7229, median = 12
  2019 total Sales = 1,249,725 ; 2020 total Sales = 366,606  (2020 < 2019, COVID dip)
</interfaces>

@src/fleet_analytics/config.py
@src/fleet_analytics/model.py
@src/fleet_analytics/transform.py
@src/fleet_analytics/export.py
@src/fleet_analytics/profile.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase-3 KPI constants to config.py</name>
  <read_first>
    - src/fleet_analytics/config.py (the file being modified — the existing Phase-2 constants block lines 68-92 is the exact in-file pattern: typed module-level constants with a `# --- section ---` divider and an inline "cited, never recalculated" justification comment; REFERENCE_YEAR lines 69-76 shows the style — do NOT duplicate it)
    - .planning/phases/03-kpi-layer-measures-spec/03-PATTERNS.md (the `config.py (MODIFY)` section, lines 86-100 — the exact ASSET_CLASS_TARGETS dict[str,int] shape)
    - .planning/phases/03-kpi-layer-measures-spec/03-CONTEXT.md (D-01 targets, D-05 snapshot dir discretion)
    - data/gold/fact_vehicle.parquet column contract (UNIT_TYPE values are the upper-case 'LIGHT DUTY' etc — the dict must map these to the target labels)
  </read_first>
  <action>
    Append a `# --- Phase-3 KPI-layer constants ---` block to config.py mirroring the existing constants style (typed, with a "cited, never recalculated — AG 2019.AU2.2 / May 2023 FSD report" justification comment). Add: `ASSET_CLASS_TARGETS: dict[str, int]` = {Light:95, Medium:92, Heavy:85, Off-Road:88, Other:90} (per D-01); a `UNIT_TYPE_TO_CLASS: dict[str, str]` mapping the actual fact_vehicle UNIT_TYPE values ('LIGHT DUTY','MEDIUM DUTY','HEAVY DUTY','OFF-ROAD','OTHER') to the target-label keys ('Light','Medium','Heavy','Off-Road','Other') so the by-class SQL can join targets in — do NOT inline either dict in SQL; bind/join them. Add `KPI_DIR: Path = PROJECT_ROOT / "data" / "kpi"` (parallel to GOLD_DIR line 79) and `KPI_TABLE_CSVS: list[str]` listing the 7 table-valued KPI base names (availability_by_class, availability_by_division, exception_list, underutilization_by_division, ferry_yoy, ferry_heatmap, sales_redemption_gap) parallel to GOLD_TABLES. Add `FERRY_COMPLETE_YEARS: tuple[int, int] = (2016, 2025)` (the inclusive complete-year YoY window, per D-10) with a comment that 2015/2026 are partial and excluded from YoY only. Do NOT add fenced implementation beyond these constant definitions.
  </action>
  <verify>
    <automated>uv run python -c "from fleet_analytics import config as c; assert c.ASSET_CLASS_TARGETS=={'Light':95,'Medium':92,'Heavy':85,'Off-Road':88,'Other':90}; assert set(c.UNIT_TYPE_TO_CLASS)=={'LIGHT DUTY','MEDIUM DUTY','HEAVY DUTY','OFF-ROAD','OTHER'}; assert len(c.KPI_TABLE_CSVS)==7; assert c.FERRY_COMPLETE_YEARS==(2016,2025); print('config OK')"</automated>
  </verify>
  <acceptance_criteria>
    - ASSET_CLASS_TARGETS equals the five audit targets 95/92/85/88/90 keyed by Light/Medium/Heavy/Off-Road/Other.
    - UNIT_TYPE_TO_CLASS keys exactly the 5 real fact_vehicle UNIT_TYPE strings.
    - KPI_DIR points to data/kpi and KPI_TABLE_CSVS has the 7 base names; FERRY_COMPLETE_YEARS == (2016, 2025).
    - No target value or class label is hard-coded inside any SQL string (constants only live in config.py).
  </acceptance_criteria>
  <done>config.py exposes ASSET_CLASS_TARGETS, UNIT_TYPE_TO_CLASS, KPI_DIR, KPI_TABLE_CSVS, FERRY_COMPLETE_YEARS; the verify import passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build the kpis.py compute module (Domain A + Domain B builders + build_all)</name>
  <files>src/fleet_analytics/kpis.py</files>
  <read_first>
    - src/fleet_analytics/model.py (PRIMARY analog — lines 1-37 module docstring + "Locked decisions encoded here" block; lines 45-95 per-builder shape `(con: duckdb.DuckDBPyConnection) -> None` single CREATE OR REPLACE; lines 169-193 build_all orchestrator with the fail-fast `assert ... f"...— regression crept in"` loop)
    - src/fleet_analytics/transform.py (lines 40-49 the `?`-bind idiom for config constants; lines 70-88 the ferry derived-field SQL whose season/daypart/day_of_week names this module groups over — no re-derivation)
    - src/fleet_analytics/export.py (lines 18-21 security note; lines 41-46 path-build/dir-ensure idiom for COPY — reuse for CSV writes in Task 3)
    - src/fleet_analytics/profile.py (line 49 the `.df().to_dict(...)` / dict-for-assertion idiom; docstring lines 10-11 "returned dict keyed for direct assertion")
    - src/fleet_analytics/config.py (the constants added in Task 1)
    - .planning/phases/03-kpi-layer-measures-spec/03-PATTERNS.md (the kpis.py section lines 26-82 — module shape, ?-bind, 209-NULL rule, build_all)
    - .planning/phases/03-kpi-layer-measures-spec/03-CONTEXT.md (D-01..D-11 — every KPI definition and the threshold/YoY/seasonality rules)
    - data/gold/*.parquet column contract (the <interfaces> block above)
  </read_first>
  <action>
    Create src/fleet_analytics/kpis.py mirroring model.py: open with `from __future__ import annotations`, `import duckdb`, `from fleet_analytics import config`, and a module docstring with a "Locked decisions encoded here" block naming the 209-NULL exclusion (denominator 4,405), the pooled-per-vehicle-mean rule (D-13), the single below-class-target threshold (D-01/D-02/D-03), and the 2016-2025 complete-years YoY rule (D-09/D-10/D-11). Read Gold via `read_parquet((config.GOLD_DIR / t).as_posix() + '.parquet')` (export.py path idiom) into CREATE OR REPLACE staging views OR query directly. Write one builder per KPI, each a single SQL statement on the shared `con`, returning a DataFrame via `con.execute(sql).df()` for table-valued KPIs:
    Domain A — `build_availability_by_class` (group fact_vehicle by UNIT_TYPE mapped to class label, AVG(AVAILABILITY_YTD) as pooled rate ignoring NULL, join ASSET_CLASS_TARGETS bound in, signed gap_to_target = rate - target, count below-target where AVAILABILITY_YTD < target); `build_availability_by_division` (by owner division name via dim_division); `build_exception_list` (units with AVAILABILITY_YTD < class target, ranked ascending by gap_to_target, carrying UNIT_NO/UNIT_TYPE/AVAILABILITY_YTD/target/gap/Utilization); `build_underutilization_by_division` (matched light-duty subset, share Utilization='Underutilized' overall and by using_division, plus the specialized split on specialized_units); a disposal-candidate screen (below class target AND Utilization='Underutilized') surfaced within exception_list as a boolean column `disposal_candidate` (framed as screening list, never a decision). Domain B — `build_ferry_yoy` (SUM Sales/Redemption per year, ROWS only for years in config.FERRY_COMPLETE_YEARS 2016..2025, YoY pct via window LAG; label 2015/2026 excluded); `build_ferry_seasonality` (pool ALL years by month/season, no annualization, D-11); `build_ferry_heatmap` (day_of_week x hour-of-day SUM Sales, all data, D-09); `build_sales_redemption_gap` (signed Sales-Redemption aggregates). Use `?`-bind for every config constant (targets, complete-year bounds) — never inline. Compute scalars (pooled overall_availability_rate, overall underutilization rate, ferry lifetime totals, max 7229 / median 12) into a flat dict in a `build_scalars(con) -> dict` returning profile.py-style keys. Add `build_all(con) -> duckdb.DuckDBPyConnection` calling each builder then a fail-fast assertion loop: assert the pooled rate is in [0,1] AND differs from the mean-of-class-means (prove pooled != average-of-averages), assert 2020 Sales < 2019 Sales, assert ferry Sales max == 7229 — each with the model.py-style `f"...— regression crept in"` message; return con. NO fenced code blocks beyond these signatures; point implementation detail to the model.py/transform.py analogs.
  </action>
  <verify>
    <automated>uv run python -c "import duckdb; from fleet_analytics import kpis; con=duckdb.connect(); kpis.build_all(con); s=kpis.build_scalars(con); assert 0<=s['overall_availability_rate']<=1; assert round(s['overall_availability_rate'],4)==0.8899; print('kpis build OK', s['overall_availability_rate'])"</automated>
  </verify>
  <acceptance_criteria>
    - kpis.build_all(con) runs clean and its in-code fail-fast asserts pass (pooled in [0,1] and != mean-of-class-means; 2020 Sales < 2019 Sales; ferry max == 7229).
    - overall_availability_rate rounds to 0.8899 (pooled per-vehicle mean over 4,405 non-null rows).
    - ferry_yoy builder returns rows ONLY for years 2016..2025 (no 2015/2026 in the YoY growth rows).
    - exception_list rows all satisfy AVAILABILITY_YTD < class target and carry a `disposal_candidate` boolean.
    - No COALESCE/fill on AVAILABILITY_YTD anywhere; no target value inlined in SQL (all `?`-bound from config).
  </acceptance_criteria>
  <done>src/fleet_analytics/kpis.py exists with per-KPI builders, build_scalars, and a build_all orchestrator whose fail-fast asserts pass; verify command prints the pooled rate 0.8899.</done>
</task>

<task type="auto">
  <name>Task 3: Write the committed KPI snapshot (kpi_values.json + 7 per-table CSVs)</name>
  <files>src/fleet_analytics/kpis.py, data/kpi/kpi_values.json, data/kpi/availability_by_class.csv, data/kpi/availability_by_division.csv, data/kpi/exception_list.csv, data/kpi/underutilization_by_division.csv, data/kpi/ferry_yoy.csv, data/kpi/ferry_heatmap.csv, data/kpi/sales_redemption_gap.csv</files>
  <read_first>
    - src/fleet_analytics/export.py (lines 41-46 — the `config.X_DIR.mkdir(parents=True, exist_ok=True)`, `(config.X_DIR / t).as_posix()`, `COPY (SELECT * FROM ...) TO '...csv' (FORMAT CSV, HEADER)` committed-artifact idiom to mirror; lines 18-21 security note — only interpolate internal config names)
    - src/fleet_analytics/profile.py (line 49 `.df().to_dict(...)`; the flat-dict-for-assertion pattern feeding json.dump)
    - src/fleet_analytics/kpis.py (the builders + build_scalars from Task 2 that feed this writer)
    - .planning/phases/03-kpi-layer-measures-spec/03-PATTERNS.md (the snapshot-artifact section lines 104-117 — JSON+CSV split, config-constant file list, security note)
    - .planning/phases/03-kpi-layer-measures-spec/03-CONTEXT.md (D-05 — scalars→JSON, table-valued→one CSV each)
  </read_first>
  <action>
    Add a `write_kpi_snapshot(con) -> None` function to kpis.py. Ensure `config.KPI_DIR.mkdir(parents=True, exist_ok=True)` (export.py idiom). For each table-valued KPI, materialize it as a CREATE OR REPLACE TABLE (named for its file) then `COPY (SELECT * FROM {name}) TO '{(config.KPI_DIR / name).as_posix()}.csv' (FORMAT CSV, HEADER)` — interpolate only the internal config-derived names (export.py security note). Iterate `config.KPI_TABLE_CSVS` so the file set is config-driven. Build the scalar dict from `build_scalars(con)` and `json.dump` it (indent=2, sorted keys for a reviewable git diff) to `(config.KPI_DIR / 'kpi_values.json')`. The JSON must hold flat/nested keys including at minimum: overall_availability_rate, availability_nonnull_n (4405), availability_null_n (209), by-class rate+target+gap entries, overall_underutilization_rate, ferry lifetime totals, ferry_sales_max (7229), ferry_sales_median (12), and the 2019/2020 totals used by the COVID-dip guard. Add a `main()`/`if __name__ == "__main__"` (or extend build_all to optionally write) so the snapshot is regenerable via `uv run python -m fleet_analytics.kpis`. Then RUN it to produce the committed files. NO fenced code beyond the function signature; reuse export.py's exact COPY idiom.
  </action>
  <verify>
    <automated>uv run python -m fleet_analytics.kpis && uv run python -c "import json,os; d=json.load(open('data/kpi/kpi_values.json')); assert 0<=d['overall_availability_rate']<=1; assert d['availability_nonnull_n']==4405 and d['availability_null_n']==209; assert d['ferry_sales_max']==7229; [os.stat('data/kpi/'+f+'.csv') for f in ['availability_by_class','availability_by_division','exception_list','underutilization_by_division','ferry_yoy','ferry_heatmap','sales_redemption_gap']]; print('snapshot OK')"</automated>
  </verify>
  <acceptance_criteria>
    - data/kpi/kpi_values.json exists, is valid JSON, and contains overall_availability_rate in [0,1], availability_nonnull_n==4405, availability_null_n==209, ferry_sales_max==7229.
    - All 7 per-table CSVs exist under data/kpi/ with headers, file set driven by config.KPI_TABLE_CSVS.
    - ferry_yoy.csv contains rows only for years 2016..2025 (partial 2015/2026 excluded from the YoY growth rows).
    - `uv run python -m fleet_analytics.kpis` regenerates the snapshot deterministically (re-running produces no diff).
    - Snapshot is committed and reviewable in git (data/kpi/ tracked).
  </acceptance_criteria>
  <done>The data/kpi/ snapshot (1 JSON + 7 CSVs) is written by write_kpi_snapshot, regenerable via the module entrypoint, and the verify command passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gold Parquet (data/gold) → kpis.py | Internal, committed, type-checked artifacts produced by Phase 2 — trusted input. No external/user data crosses here. |
| kpis.py → data/kpi snapshot files | Internal write of computed analytics to committed files. No external consumer at write time. |

This phase is a local, offline, single-machine data pipeline: no network surface,
no authentication, no untrusted user input. It reads committed Parquet and writes
committed JSON/CSV. The realistic attack surface is minimal; threats are stated
honestly below rather than invented.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | f-string SQL in COPY / read_parquet | mitigate | Interpolate ONLY internal `config.KPI_TABLE_CSVS` / `config.KPI_DIR` / `config.GOLD_DIR` names into SQL strings — never an external or user-supplied value (mirrors export.py lines 18-21). |
| T-03-02 | Information Disclosure | committed snapshot files | accept | Snapshot holds aggregate fleet/ferry analytics from public City of Toronto Open Data — no PII, secrets, or sensitive values. Low-value target; committing it is the intended deliverable. |
| T-03-03 | Tampering (supply chain) | python deps | accept | No NEW package installs in this plan (duckdb/pandas/json already vetted and pinned in Phase 1). No package-legitimacy gate required. |
</threat_model>

<verification>
- `uv run python -m fleet_analytics.kpis` runs clean and regenerates the snapshot.
- The in-code fail-fast asserts in `kpis.build_all` pass (pooled-mean bounds + pooled != average-of-averages; 2020 < 2019; max 7229).
- All snapshot files exist under data/kpi/ and parse.
</verification>

<success_criteria>
- config.py carries the audit-cited asset-class targets and KPI constants (no inlined thresholds).
- kpis.py computes every Domain A and Domain B KPI per D-01..D-11 with the 209-NULL exclusion and pooled-mean rule.
- The committed data/kpi/ snapshot (kpi_values.json + 7 CSVs) is the falsifiable ground-truth the dashboard must reproduce.
</success_criteria>

<output>
Create `.planning/phases/03-kpi-layer-measures-spec/03-01-SUMMARY.md` when done.
</output>
