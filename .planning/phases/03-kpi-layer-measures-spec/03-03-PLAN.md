---
phase: 03-kpi-layer-measures-spec
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - deliverables/kpi_definitions.md
  - deliverables/measures_spec.md
autonomous: true
requirements:
  - KPI-02

must_haves:
  truths:
    - "deliverables/kpi_definitions.md states plain-language formulas + audit benchmarks for every Domain A/B KPI and includes the 5.8% (computed) vs ~14% (cited audit) underutilization reconciliation note"
    - "kpi_definitions.md carries the BA narrative and a KPI↔AG-theme traceability mapping (downtime AU2.2 / underutilization AU2.3)"
    - "deliverables/measures_spec.md is grouped by domain→KPI; each KPI subsection has a Measure name | DAX | SQL validation value | Notes table"
    - "Every DAX measure in measures_spec.md carries the SQL validation value sourced from the committed snapshot (kpi_values.json / per-table CSVs)"
    - "Both docs close with the Sources & Licence footer (Open Data, May 2023 FSD report, AG 2019.AU2.2/.3, OGL-Toronto)"
  artifacts:
    - path: "deliverables/kpi_definitions.md"
      provides: "Plain-language KPI formulas, audit benchmarks, 5.8%-vs-14% reconciliation, BA narrative + AG-theme traceability"
      min_lines: 60
      contains: "Reconciliation"
    - path: "deliverables/measures_spec.md"
      provides: "DAX-ready measures spec: domain→KPI sections with Measure name | DAX | SQL validation value | Notes tables"
      min_lines: 60
      contains: "SQL validation value"
  key_links:
    - from: "deliverables/measures_spec.md"
      to: "data/kpi/kpi_values.json"
      via: "each SQL validation value cell transcribes a snapshot value"
      pattern: "validation"
    - from: "deliverables/kpi_definitions.md"
      to: "deliverables/dq_report.md"
      via: "extend/cross-reference the existing 5.8%-vs-14% reconciliation table"
      pattern: "14%"
---

<objective>
Author the two Phase 3 deliverable docs that turn the computed KPI snapshot into a
panel-ready definitions doc and a Phase-4 build contract (D-07/D-08):
`deliverables/kpi_definitions.md` (plain-language formulas, audit benchmarks, the
5.8%-vs-14% reconciliation, BA narrative + KPI↔AG-theme traceability) and
`deliverables/measures_spec.md` (domain→KPI sections, each with a
`Measure name | DAX | SQL validation value | Notes` table whose validation values
come from the committed snapshot).

Purpose: These docs are the handoff surface Phase 4 (report spec) and Phase 5
(narratives) consume — every DAX measure becomes falsifiable against a snapshot value.
Output: `deliverables/kpi_definitions.md` and `deliverables/measures_spec.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-kpi-layer-measures-spec/03-CONTEXT.md
@.planning/phases/03-kpi-layer-measures-spec/03-PATTERNS.md

<interfaces>
<!-- Snapshot values (from Plan 01) every "SQL validation value" cell must cite. -->
data/kpi/kpi_values.json: overall_availability_rate (~0.8899),
  overall_underutilization_rate (~0.0572 → framed "5.8%"), ferry_sales_max (7229),
  ferry_sales_median (12), by-class rate/target/gap (Light 95 / Medium 92 /
  Heavy 85 / Off-Road 88 / Other 90), 2019/2020 ferry totals.
data/kpi/*.csv: availability_by_class, availability_by_division, exception_list,
  underutilization_by_division, ferry_yoy, ferry_heatmap, sales_redemption_gap.

DAX hints (planner discretion on naming — keep Power-BI-friendly, use DIVIDE for ratios):
  pooled grand-total availability = DIVIDE(SUM(AVAILABILITY_YTD), COUNTROWS(non-null))
  — NOT AVERAGEX over class averages (mirror the pooled-mean rule).

AG themes for traceability: downtime = AU2.2 ; underutilization = AU2.3.
</interfaces>

@deliverables/dq_report.md
@deliverables/data_dictionary.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write deliverables/kpi_definitions.md (formulas, benchmarks, reconciliation, traceability)</name>
  <files>deliverables/kpi_definitions.md</files>
  <read_first>
    - deliverables/dq_report.md (the PRIMARY analog — lines 1-16 the titled header block `**Project:** / **Scope:** / **Method:** / **Snapshot pull date:**` + the "Why this report exists" callout + numbered `## N.` sections; lines 84-90 the EXISTING 5.8%-vs-14% reconciliation table to extend/cross-reference, and the computed-vs-cited inline flagging)
    - deliverables/data_dictionary.md (lines 84-90 the `## Sources & Licence` footer to reuse verbatim)
    - .planning/phases/03-kpi-layer-measures-spec/03-CONTEXT.md (D-01..D-11 KPI definitions; D-02 disposal-candidate framed as SME screening list; the AG-theme traceability requirement)
    - .planning/phases/03-kpi-layer-measures-spec/03-PATTERNS.md (the kpi_definitions.md section lines 153-157)
    - data/kpi/kpi_values.json (the computed values to cite in the formulas)
  </read_first>
  <action>
    Create deliverables/kpi_definitions.md mirroring dq_report.md's header block (Project/Scope/Method/Snapshot pull date) and "Why this doc exists" callout. Write numbered `## N.` sections for Domain A (overall availability rate as the pooled per-vehicle mean — state the 4,405-non-null denominator and the 209-NULL exclusion explicitly; availability by asset class vs the audit targets 95/92/85/88/90 with signed gap-to-target per D-03; exception list of critically-low units = below class target; underutilization rate overall and by using division; specialized split; the disposal-candidate cross-measure = below-class-target AND Underutilized — phrased as a SCREENING LIST FOR SME REVIEW, never a disposal decision per D-02) and Domain B (ferry lifetime/period totals on ALL data; YoY on complete years 2016-2025 only with 2015/2026 labeled partial; seasonality pooling all years by month; DoW×hour heatmap; signed sales-redemption gap; distribution stats max≈7229/median≈12). Each KPI: plain-language formula + audit benchmark, with cited figures flagged inline (AG 2019.AU2.2 downtime / AU2.3 underutilization — CITED, never recalculated) exactly like dq_report.md distinguishes computed vs cited. Include the 5.8%-(computed)-vs-~14%-(cited audit Mar 2023) reconciliation note — cross-reference/extend the existing table at dq_report.md lines 84-90, do NOT recompute thresholds. Add a `## KPI ↔ AG Theme Traceability` table mapping each KPI to AU2.2 (downtime/availability) or AU2.3 (underutilization). Close with the `## Sources & Licence` footer copied from data_dictionary.md. NO fenced code; prose + Markdown tables only.
  </action>
  <verify>
    <automated>uv run python -c "t=open('deliverables/kpi_definitions.md',encoding='utf-8').read(); assert 'Sources' in t and 'Licence' in t; assert '14%' in t; assert 'AU2.2' in t and 'AU2.3' in t; assert 'screening' in t.lower() or 'sme' in t.lower(); assert '4,405' in t or '4405' in t; print('kpi_definitions OK len',len(t))"</automated>
  </verify>
  <acceptance_criteria>
    - kpi_definitions.md has a dq_report-style header block, numbered Domain A and Domain B sections, and a Sources & Licence footer.
    - The 5.8%-vs-~14% reconciliation note is present and cross-references the existing dq_report.md table (the literal "14%" appears).
    - Disposal-candidate text reads as a screening list for SME review (the words "screening"/"SME" appear), never a disposal decision.
    - The 4,405 non-null denominator / 209-NULL exclusion is stated for the availability rate.
    - A KPI↔AG-theme traceability table maps KPIs to AU2.2 / AU2.3 (both appear).
  </acceptance_criteria>
  <done>deliverables/kpi_definitions.md exists with formulas, benchmarks, the reconciliation note, the SME-screening framing, the AG-theme traceability table, and the Sources footer; verify passes.</done>
</task>

<task type="auto">
  <name>Task 2: Write deliverables/measures_spec.md (DAX + SQL validation value per KPI)</name>
  <files>deliverables/measures_spec.md</files>
  <read_first>
    - deliverables/data_dictionary.md (the PRIMARY analog — lines 16-41 / 45-59 / 63-73 the section-per-entity, table-per-section idiom to mirror for domain→KPI grouping; lines 84-90 the Sources & Licence footer)
    - deliverables/dq_report.md (lines 1-16 the header block to reuse)
    - .planning/phases/03-kpi-layer-measures-spec/03-CONTEXT.md (D-08 — group by domain→KPI; each KPI a subsection with a `Measure name | DAX | SQL validation value | Notes` table; D-07 the measures spec consumes D-05 snapshot values)
    - .planning/phases/03-kpi-layer-measures-spec/03-PATTERNS.md (the measures_spec.md section lines 161-165 — table-per-section, every SQL-validation-value cell cites the snapshot)
    - data/kpi/kpi_values.json AND the 7 per-table CSVs (the SOURCE of every "SQL validation value" cell — read the actual persisted values, do not invent)
    - deliverables/kpi_definitions.md (the companion doc from Task 1 — keep measure semantics consistent)
  </read_first>
  <action>
    Create deliverables/measures_spec.md mirroring data_dictionary.md's table-per-section layout, with a dq_report-style header block. Structure per D-08: a `## Fleet Maintenance (Domain A)` section and a `## Ferry Operations (Domain B)` section, each KPI a `### ` subsection containing a Markdown table with columns `Measure name | DAX | SQL validation value | Notes`. Cover every KPI from kpi_definitions.md. For the DAX cells use Power-BI-friendly measure names (planner discretion) and `DIVIDE` for ratios; the overall availability measure must be the pooled grand-total form (DIVIDE(SUM, COUNTROWS over non-null), NOT AVERAGEX over class averages) — note this in the Notes cell. Each "SQL validation value" cell MUST transcribe the actual value persisted in data/kpi/kpi_values.json or the relevant per-table CSV (e.g. overall availability ≈ 0.8899; overall underutilization ≈ 5.7%/"5.8%"; ferry max 7229 / median 12; per-class rate vs target 95/92/85/88/90) so every measure is falsifiable against ground truth. Notes cells flag the 209-NULL exclusion, the complete-years YoY window, and the SME-screening framing for the disposal-candidate measure. Close with the `## Sources & Licence` footer from data_dictionary.md. NO fenced code beyond DAX expressions inside the table cells; Markdown tables only.
  </action>
  <verify>
    <automated>uv run python -c "t=open('deliverables/measures_spec.md',encoding='utf-8').read(); assert 'SQL validation value' in t; assert 'DAX' in t and 'DIVIDE' in t; assert 'Fleet Maintenance' in t and 'Ferry' in t; assert '7229' in t or '7,229' in t; assert 'Sources' in t and 'Licence' in t; print('measures_spec OK len',len(t))"</automated>
  </verify>
  <acceptance_criteria>
    - measures_spec.md is grouped Domain A (Fleet Maintenance) then Domain B (Ferry), each KPI a subsection with a `Measure name | DAX | SQL validation value | Notes` table.
    - Every "SQL validation value" cell transcribes a value from the committed snapshot (the literal 7229 and ≈0.8899 appear; per-class targets 95/92/85/88/90 present).
    - The overall availability DAX uses the pooled grand-total form with DIVIDE (noted as NOT average-of-averages).
    - The disposal-candidate measure's Notes cell frames it as an SME screening list.
    - The Sources & Licence footer is present.
  </acceptance_criteria>
  <done>deliverables/measures_spec.md exists with domain→KPI tables pairing DAX + snapshot-sourced SQL validation values + notes, and the Sources footer; verify passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data/kpi snapshot → deliverable docs | Internal committed values transcribed into committed Markdown. No external input, no network, no auth, no executable surface. |

This plan authors two static Markdown documents from internal computed values. No
network surface, no untrusted input, no secrets, no code execution. Threat surface
is effectively nil.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Repudiation / Integrity | doc values drift from snapshot | mitigate | Every "SQL validation value" is transcribed directly from data/kpi/kpi_values.json / the CSVs (single source of truth); Plan 02's snapshot-as-contract tests keep the snapshot honest, so the docs inherit that guarantee. |
| T-03-08 | Information Disclosure | committed Markdown | accept | Docs contain public-data aggregates and audit-cited benchmarks only — no PII/secrets. Intended public deliverable. |
| T-03-09 | Tampering (supply chain) | python deps | accept | No package installs in this plan (docs only). No legitimacy gate required. |
</threat_model>

<verification>
- Both verify commands pass (header block, required figures, AG themes, Sources footer present).
- Spot-check: every SQL validation value in measures_spec.md matches a value in data/kpi/kpi_values.json or a CSV.
</verification>

<success_criteria>
- kpi_definitions.md gives panel-ready formulas + benchmarks + the 5.8%-vs-14% reconciliation + AG-theme traceability.
- measures_spec.md pairs every KPI's DAX with its snapshot-sourced SQL validation value — the Phase-4 build contract.
</success_criteria>

<output>
Create `.planning/phases/03-kpi-layer-measures-spec/03-03-SUMMARY.md` when done.
</output>
