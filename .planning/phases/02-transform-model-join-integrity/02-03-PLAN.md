---
phase: 02-transform-model-join-integrity
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/fleet_analytics/export.py
  - tests/test_export.py
  - deliverables/dq_report.md
autonomous: true
requirements: [MODEL-04]
user_setup: []

must_haves:
  truths:
    - "All five Gold tables export to data/gold/ as type-preserving Parquet plus readable CSV (10 files)"
    - "The exported fact_vehicle.parquet preserves AVAILABILITY_YTD as DOUBLE with 209 genuine NULLs, IN_SERV_DT as DATE, and is_weekend as boolean"
    - "The exported fact_vehicle.csv re-reads with NULL count == 209 (blanks stay NULL, never 0-filled)"
    - "The 6 unmatched utilization rows AND the 44 alphanumeric availability units are documented as DQ findings in deliverables/dq_report.md"
  artifacts:
    - path: "src/fleet_analytics/export.py"
      provides: "write_gold(con) — COPY each Gold table to Parquet + CSV under config.GOLD_DIR"
      exports: ["write_gold"]
      min_lines: 25
    - path: "tests/test_export.py"
      provides: "MODEL-04 roundtrip: Parquet type/null preservation + CSV 209-null preservation"
      min_lines: 30
    - path: "deliverables/dq_report.md"
      provides: "6-unmatched + 44-alphanumeric-unit DQ findings (Phase-2 addendum)"
      contains: "unmatched"
  key_links:
    - from: "src/fleet_analytics/export.py write_gold"
      to: "config.GOLD_DIR / config.GOLD_TABLES"
      via: "COPY (SELECT * FROM {t}) TO '{GOLD_DIR}/{t}.parquet' (FORMAT PARQUET)"
      pattern: "FORMAT PARQUET"
    - from: "tests/test_export.py"
      to: "data/gold/fact_vehicle.parquet + .csv"
      via: "second :memory: con re-reads the exported files"
      pattern: "read_parquet|read_csv"
---

<objective>
Export the five Gold tables to `data/gold/` as type-preserving Parquet (primary) plus readable CSV (secondary), with a roundtrip test proving type and 209-null preservation (MODEL-04). Document the 6-unmatched and 44-alphanumeric-unit DQ findings in `deliverables/dq_report.md`.

Purpose: This is the Power BI handoff surface (Phases 3–4 consume these Parquet files). Type fidelity — especially the 209 NULLs surviving export — is the MODEL-04 contract.
Output: `export.py`, `test_export.py`, DQ findings addendum in `dq_report.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/02-transform-model-join-integrity/02-CONTEXT.md
@.planning/phases/02-transform-model-join-integrity/02-RESEARCH.md
@.planning/phases/02-transform-model-join-integrity/02-PATTERNS.md
@.planning/phases/02-transform-model-join-integrity/02-VALIDATION.md
@.planning/phases/02-transform-model-join-integrity/02-02-SUMMARY.md
@deliverables/dq_report.md

<interfaces>
<!-- Contracts from Plans 02-01 / 02-02 this plan consumes. -->

From src/fleet_analytics/config.py (Plan 01 output):
```python
GOLD_DIR: Path            # <PROJECT_ROOT>/data/gold
GOLD_TABLES: list[str]    # ["dim_division","fact_vehicle","fact_ferry","dim_date","dim_time"]
```

From src/fleet_analytics/model.py (Plan 02 output):
```python
def build_all(con) -> con   # creates the five Gold tables + dq_unmatched_utilization on con
# fact_vehicle.AVAILABILITY_YTD: DOUBLE with 209 NULLs; IN_SERV_DT: DATE; is_weekend present
# dq_unmatched_utilization: the 6 unmatched utilization rows
```

From tests/conftest.py (Plan 02 output):
```python
@pytest.fixture(scope="session")
def gold(con):  # builds transform + model on con
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create export.py — Parquet + CSV COPY for all five Gold tables (MODEL-04)</name>
  <files>src/fleet_analytics/export.py</files>
  <read_first>
    - src/fleet_analytics/ingest.py (full file — mirror the docstring style and the `con.execute` loop shape)
    - src/fleet_analytics/config.py (GOLD_DIR, GOLD_TABLES, the csv_path path-derivation idiom at lines 33-35)
    - .planning/phases/02-transform-model-join-integrity/02-RESEARCH.md Example 7 (lines 398-412, COPY loop) and the Security note (lines 528-532, internal-constant table names are safe to interpolate)
    - .planning/phases/02-transform-model-join-integrity/02-PATTERNS.md § "export.py" (lines 105-118)
  </read_first>
  <behavior>
    - write_gold(con) creates config.GOLD_DIR (mkdir parents=True, exist_ok=True) then COPYs each of the five Gold tables to {GOLD_DIR}/{table}.parquet (FORMAT PARQUET) and {table}.csv (FORMAT CSV, HEADER)
    - After write_gold, data/gold/ contains exactly 10 files (5 .parquet + 5 .csv)
    - fact_vehicle.parquet re-reads with AVAILABILITY_YTD DOUBLE + 209 NULLs, IN_SERV_DT DATE, is_weekend boolean
    - fact_vehicle.csv re-reads with NULL count == 209 (blanks not 0-filled)
  </behavior>
  <action>
    Create `src/fleet_analytics/export.py` mirroring `ingest.py`'s module shape and docstring (name MODEL-04). Define `write_gold(con: duckdb.DuckDBPyConnection) -> None` that:
    - ensures the output dir exists: `config.GOLD_DIR.mkdir(parents=True, exist_ok=True)`.
    - loops over `config.GOLD_TABLES` and for each runs one `COPY (SELECT * FROM {t}) TO '{p}.parquet' (FORMAT PARQUET)` and one `COPY (SELECT * FROM {t}) TO '{p}.csv' (FORMAT CSV, HEADER)` where `p = (config.GOLD_DIR / t).as_posix()` (embed 02-RESEARCH.md Example 7). Table names come from the internal `GOLD_TABLES` constant only (never user input) so the f-string interpolation is safe per the Security note.
    Do not aggregate or alter any column — COPY preserves DOUBLE+NULLs / DATE / boolean natively (verified). No fenced code in this action — reuse the verified loop from RESEARCH Example 7.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_export.py</automated>
  </verify>
  <acceptance_criteria>
    - `export.write_gold(con)` runs after model.build_all and creates `data/gold/` if absent
    - `data/gold/` contains fact_vehicle, fact_ferry, dim_division, dim_date, dim_time as both .parquet and .csv (10 files)
    - Re-reading fact_vehicle.parquet on a fresh con yields AVAILABILITY_YTD DOUBLE with exactly 209 NULLs and IN_SERV_DT typed DATE
    - `export.py` contains `FORMAT PARQUET` and `FORMAT CSV, HEADER` and no COALESCE/aggregation
    - `uv run pytest -q tests/test_export.py` exits 0
  </acceptance_criteria>
  <done>All five Gold tables export to Parquet + CSV with types and 209 NULLs preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Write test_export.py — Parquet/CSV roundtrip type + null preservation (MODEL-04 gate)</name>
  <files>tests/test_export.py</files>
  <read_first>
    - tests/test_nulls.py (full file — the `test_no_null_became_zero` roundtrip pattern at lines 54-94: open a second :memory: con in try/finally, re-read the artifact, assert preservation)
    - src/fleet_analytics/export.py (Task 1 output — write_gold signature and output paths)
    - src/fleet_analytics/config.py (GOLD_DIR)
    - .planning/phases/02-transform-model-join-integrity/02-PATTERNS.md § "test_export.py" (lines 274-297)
    - .planning/phases/02-transform-model-join-integrity/02-VALIDATION.md (MODEL-04 rows, lines 56-57)
  </read_first>
  <action>
    Create `tests/test_export.py` taking the `gold` fixture. In the test(s), call `export.write_gold(con)` (writing to `config.GOLD_DIR`, or pass/redirect to a tmp dir if you prefer isolation — but the node IDs below are required). Then open a SECOND `:memory:` connection in a `try/finally` (mirror test_nulls.py lines 64-87) and re-read the exported files:
    - `test_parquet_types` — `read_parquet('data/gold/fact_vehicle.parquet')`; assert AVAILABILITY_YTD column is DOUBLE with exactly 209 NULLs, IN_SERV_DT is DATE, is_weekend is boolean (use DuckDB `typeof(...)` or a describe query plus a null-count query).
    - `test_csv_nulls` — `read_csv('data/gold/fact_vehicle.csv', ...)`; assert AVAILABILITY_YTD NULL count == 209 (blanks stay NULL, never 0). Mirror the test_nulls.py raw-reread idiom; alias the count as `null_ct` (never `nulls`/`both`, Pitfall 6).
    Use the exact node IDs `test_parquet_types` and `test_csv_nulls` (consumed verbatim by 02-VALIDATION.md).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_export.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_export.py::test_parquet_types -x` exits 0
    - `uv run pytest tests/test_export.py::test_csv_nulls -x` exits 0
    - test asserts the Parquet AVAILABILITY_YTD null count == 209 and the CSV reread null count == 209
    - `uv run pytest -q` full suite green (all four MODEL requirements + Phase-1 regression guards)
  </acceptance_criteria>
  <done>MODEL-04 roundtrip gate green; types and 209 NULLs proven preserved across Parquet and CSV.</done>
</task>

<task type="auto">
  <name>Task 3: Document the 6-unmatched + 44-alphanumeric DQ findings (D-03, D-06)</name>
  <files>deliverables/dq_report.md</files>
  <read_first>
    - deliverables/dq_report.md (full file — match the existing Phase-1 finding format/headings; append a Phase-2 addendum section, do not rewrite earlier findings)
    - .planning/phases/02-transform-model-join-integrity/02-CONTEXT.md D-03 (6 unmatched), D-06 (division-name reconciliation + truncation), and § Open Questions / Specifics (44 alphanumeric units, truncated OWNER_DIVISION)
    - .planning/phases/02-transform-model-join-integrity/02-RESEARCH.md Example 5 DQ note (lines 370-371 — clean 21/20 reconciliation, zero unreconciled names) and Open Question 1 (lines 438-441, 44-unit note recommendation)
    - .planning/phases/02-transform-model-join-integrity/02-VALIDATION.md Manual-Only Verifications (lines 84-88)
  </read_first>
  <action>
    Append a Phase-2 DQ-findings addendum to `deliverables/dq_report.md` (match the existing heading/format style). Document three findings, each with rationale and the count/mechanism that produced it:
    - The 6 unmatched utilization rows: utilization records with no availability match, surfaced via the anti-join (`dq_unmatched_utilization`), guarded by `test_unmatched_6`. Note they fall outside the availability-anchored `fact_vehicle` by design (D-03), and reconcile as 2,080 matched + 6 unmatched == 2,086.
    - The 44 alphanumeric availability `UNIT_NO` values (e.g. `296011A`, `CLAW10`): real fleet units carrying non-integer IDs, excluded from the integer join key by design via `TRY_CAST`, surviving in `fact_vehicle` with a NULL `unit_key_int` and never matching utilization (Open Question 1 recommendation).
    - Division-name reconciliation (D-06): the normalized distinct union is exactly 21 (= owner count) because all 20 using-division names are a normalized subset of the 21 owner names — zero unreconciled names (a clean DQ result). Still note the verbatim source truncations (e.g. `ENVIRONMENT, CLIMATE & FORESTR`) are preserved, not "fixed".
    Cite this is a Phase-2 finding; keep numbers consistent with the verified targets. Prose deliverable — no code blocks required beyond the inline identifiers.
  </action>
  <verify>
    <automated>uv run python -c "t=open('deliverables/dq_report.md',encoding='utf-8').read(); assert 'unmatched' in t.lower() and '44' in t and ('296011A' in t or 'alphanumeric' in t.lower()) and 'CLIMATE' in t.upper(); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `deliverables/dq_report.md` documents the 6 unmatched utilization rows with the anti-join rationale and the 2,080+6==2,086 reconciliation
    - It documents the 44 alphanumeric availability units and the TRY_CAST/NULL-key-by-design rationale
    - It documents the clean 21/20 division-name reconciliation and the preserved verbatim truncation (`ENVIRONMENT, CLIMATE & FORESTR`)
    - The grep-assert command above exits 0
  </acceptance_criteria>
  <done>All three Phase-2 DQ findings documented; the MODEL-03 manual-only documentation verification is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gold tables → local Parquet/CSV files | Output is written to the local filesystem (data/gold/); table names in COPY come from the internal GOLD_TABLES constant, never external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | export.py COPY statements | mitigate | Table names interpolated into COPY come only from the internal config.GOLD_TABLES list; output paths derived from config.GOLD_DIR — no external value reaches the SQL string (Security note, RESEARCH lines 528-532). |
| T-02-06 | Tampering/Repudiation | 209-null preservation through export | mitigate | The Parquet/CSV roundtrip test (test_parquet_types / test_csv_nulls) is the integrity control proving no 0-fill occurred on export. |

No authentication, session, network, secret, or untrusted-input surface. No high-severity threats.
</threat_model>

<verification>
- `uv run pytest -q tests/test_export.py` green (MODEL-04 Parquet + CSV roundtrip)
- `uv run pytest -q` full suite green (all four MODEL requirements + Phase-1 guards)
- `data/gold/` contains five .parquet + five .csv files
- `deliverables/dq_report.md` contains the three Phase-2 DQ findings
</verification>

<success_criteria>
- MODEL-04 satisfied: all five Gold tables export as type-preserving Parquet plus readable CSV; AVAILABILITY_YTD DOUBLE with 209 NULLs preserved through both formats (tested)
- 6-unmatched, 44-alphanumeric, and division-reconciliation DQ findings documented (D-03, D-06)
- Phase-2 Gold layer ready for Phase-3 KPI consumption and Phase-4 Power BI import
</success_criteria>

<output>
Create `.planning/phases/02-transform-model-join-integrity/02-03-SUMMARY.md` when done
</output>
