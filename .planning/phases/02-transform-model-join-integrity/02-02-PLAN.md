---
phase: 02-transform-model-join-integrity
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/fleet_analytics/model.py
  - tests/conftest.py
  - tests/test_join_integrity.py
  - tests/test_dimensions.py
autonomous: true
requirements: [MODEL-02, MODEL-03]
user_setup: []

must_haves:
  truths:
    - "fact_vehicle is availability-anchored (4,614 rows, no fan-out) with utilization LEFT-joined (D-01); 2,080 rows carry a non-null Utilization flag and join-integrity asserts matched == 2,080 / no NaN key / unique fact key (D-02)"
    - "The 6 unmatched utilization rows are surfaced by an anti-join and reconcile as 2,080 + 6 == 2,086 (D-03)"
    - "dim_division is one conformed dimension of 21 rows with unique surrogate keys, built from the normalized distinct union of owner + using division names (D-04/D-06)"
    - "fact_vehicle carries two role-playing FKs: owner_division_key (always populated) and using_division_key (nullable, light-duty only)"
    - "dim_date is gapless 2015→2026 (4,383 rows) and dim_time is exactly 96 rows"
    - "AVAILABILITY_YTD still has 209 NULLs in fact_vehicle (null discipline preserved through the join)"
  artifacts:
    - path: "src/fleet_analytics/model.py"
      provides: "five Gold table builders + build_all(con) with a fail-fast Gold count loop"
      exports: ["build_all"]
      min_lines: 80
    - path: "tests/test_join_integrity.py"
      provides: "MODEL-03 hard gate: matched/unmatched/fan-out/unique-key/null-key asserts"
      min_lines: 40
    - path: "tests/test_dimensions.py"
      provides: "MODEL-02 dim_date gapless / dim_time 96 / dim_division conformed 21 asserts"
      min_lines: 30
  key_links:
    - from: "src/fleet_analytics/model.py fact_vehicle"
      to: "stg_availability LEFT JOIN stg_utilization"
      via: "ON unit_key_int (availability-anchored)"
      pattern: "LEFT JOIN"
    - from: "src/fleet_analytics/model.py fact_vehicle FKs"
      to: "dim_division.division_key"
      via: "normalized-name join for owner_division_key + using_division_key"
      pattern: "division_key"
    - from: "tests/conftest.py gold fixture"
      to: "model.build_all"
      via: "fixture body call after transform.build_all"
      pattern: "model\\.build_all"
---

<objective>
Build the five Gold tables in `model.py` — the conformed `dim_division` (21), the availability-anchored `fact_vehicle` (4,614) with the flagship availability⋈utilization LEFT JOIN and role-playing division FKs, `fact_ferry` (272,529), gapless `dim_date` (4,383), and 96-row `dim_time` — plus the MODEL-03 join-integrity hard gate and MODEL-02 dimension tests. Wire the `gold` fixture to also run `model.build_all`.

Purpose: This is the critical-path node. MODEL-03 (the value-added join most candidates miss) and MODEL-02 (the star schema) are won or lost here. The join-integrity test is the non-negotiable < 5s gate.
Output: `model.py`, extended `gold` fixture, `test_join_integrity.py`, `test_dimensions.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/02-transform-model-join-integrity/02-CONTEXT.md
@.planning/phases/02-transform-model-join-integrity/02-RESEARCH.md
@.planning/phases/02-transform-model-join-integrity/02-PATTERNS.md
@.planning/phases/02-transform-model-join-integrity/02-VALIDATION.md
@.planning/phases/02-transform-model-join-integrity/02-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by Plan 02-01 (transform layer) this plan consumes. -->

From src/fleet_analytics/transform.py (Plan 01 output):
```python
def build_all(con) -> con   # creates stg_availability, stg_utilization, stg_ferry
# stg_availability: bronze_availability + unit_key_int (TRY_CAST, NULL for 44 alnum units), fleet_age, OWNER_DIVISION (raw, truncated names preserved)
# stg_utilization:  bronze_utilization  + unit_key_int (0 NULLs), Utilization, "Specialized units", REF_USING_DIV
# stg_ferry:        bronze_ferry + ts_15, season, daypart, day_of_week, is_weekend, sales_redemption_gap
```

From src/fleet_analytics/config.py (Plan 01 output):
```python
GOLD_TABLES: list[str]
GOLD_EXPECTED_ROWS: dict[str,int]  # dim_division:21, fact_vehicle:4614, fact_ferry:272529, dim_date:4383, dim_time:96
```

From tests/conftest.py (Plan 01 output — extend the same gold fixture):
```python
@pytest.fixture(scope="session")
def gold(con):  # currently calls transform.build_all(con); add model.build_all(con)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create model.py — five Gold tables + the flagship join (MODEL-02, MODEL-03)</name>
  <files>src/fleet_analytics/model.py</files>
  <read_first>
    - src/fleet_analytics/ingest.py (full file — mirror the per-table `CREATE OR REPLACE TABLE AS SELECT` builders, `build_all`-style orchestrator returning con, and the fail-fast count-assertion loop at lines 70-75)
    - src/fleet_analytics/transform.py (Plan 01 output — the stg_* tables and their columns this module consumes)
    - .planning/phases/02-transform-model-join-integrity/02-RESEARCH.md Example 2 (lines 300-323, fact_vehicle LEFT JOIN + anti-join), Example 5 (lines 352-370, dim_division), Example 6 (lines 372-396, dim_date + dim_time)
    - .planning/phases/02-transform-model-join-integrity/02-RESEARCH.md Anti-Patterns + Pitfalls 1, 2, 3 (lines 212-264)
    - .planning/phases/02-transform-model-join-integrity/02-PATTERNS.md § "model.py" (lines 80-101)
    - .planning/phases/02-transform-model-join-integrity/02-CONTEXT.md D-01..D-06
  </read_first>
  <behavior>
    - dim_division == 21 rows; division_key is a unique ROW_NUMBER() surrogate over the normalized distinct union of OWNER_DIVISION + REF_USING_DIV (D-04/D-06)
    - fact_vehicle == 4,614 rows (availability-anchored LEFT JOIN; no fan-out; the 44 alnum units survive)
    - fact_vehicle has 2,080 rows with non-null Utilization; owner_division_key always populated; using_division_key NULL for non-light-duty (D-05)
    - anti-join (stg_utilization LEFT JOIN stg_availability WHERE availability key IS NULL) == 6; 2,080 + 6 == 2,086
    - fact_ferry == 272,529 rows carrying ts_15 + derived fields
    - dim_date == 4,383 gapless rows (2015-01-01 → 2026-12-31) with natural DATE date_key; dim_time == 96 rows
    - AVAILABILITY_YTD null count in fact_vehicle == 209 (no COALESCE through the join)
  </behavior>
  <action>
    Create `src/fleet_analytics/model.py` mirroring `ingest.py`. Docstring names MODEL-02/MODEL-03 and D-01..D-06. Define one private builder per Gold table, each a single `CREATE OR REPLACE TABLE`:
    - `dim_division` — embed 02-RESEARCH.md Example 5: `upper(trim(regexp_replace(col, '\s+', ' ', 'g')))` over OWNER_DIVISION (stg_availability) UNION over REF_USING_DIV (stg_utilization), `ROW_NUMBER() OVER (ORDER BY division_name)` surrogate `division_key`. Keep truncated names verbatim (D-06) — do NOT force-map spellings.
    - `fact_vehicle` — embed 02-RESEARCH.md Example 2: `stg_availability a LEFT JOIN stg_utilization u ON a.unit_key_int = u.unit_key_int`, anchored on availability (D-01). Carry `u.Utilization`, `u."Specialized units" AS specialized_units`, `u.REF_USING_DIV`, plus the two role-playing FKs (D-05): `owner_division_key` by joining the normalized OWNER_DIVISION name to dim_division (always populated) and `using_division_key` by joining the normalized REF_USING_DIV name (NULL for non-light-duty). NEVER `INNER JOIN` and NEVER `WHERE unit_key_int IS NOT NULL` before the fact build (Anti-patterns / Pitfall 1). No COALESCE on AVAILABILITY_YTD (Pitfall 3).
    - `dq_unmatched_utilization` — the anti-join from Example 2 capturing the 6 unmatched rows (D-03); these fall outside fact_vehicle by design (Pitfall 2).
    - `fact_ferry` — `SELECT * FROM stg_ferry` (full 15-min grain, 272,529 rows; aggregation is a Phase-3 concern per RESEARCH Open Question 2).
    - `dim_date` and `dim_time` — embed 02-RESEARCH.md Example 6: `generate_series` for both; natural DATE `date_key` for dim_date, `ROW_NUMBER()` `time_key` for dim_time.
    Add `build_all(con) -> con` calling the builders in dependency order (dim_division before fact_vehicle so the FK joins resolve) then a fail-fast count loop over `config.GOLD_EXPECTED_ROWS` (mirror ingest.py lines 70-75) so a regression fails in code, not only in pytest. Return con. No fenced code in this action — reuse the verified SQL bodies from the cited RESEARCH examples.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_join_integrity.py tests/test_dimensions.py</automated>
  </verify>
  <acceptance_criteria>
    - `model.build_all(con)` runs after `transform.build_all(con)` and returns con; its in-code count loop passes against config.GOLD_EXPECTED_ROWS
    - `SELECT COUNT(*) FROM fact_vehicle` == 4614 and `COUNT(DISTINCT UNIT_NO)` == 4614 (no fan-out, unique key)
    - `SELECT COUNT(*) FROM fact_vehicle WHERE Utilization IS NOT NULL` == 2080
    - `SELECT COUNT(*) FROM dq_unmatched_utilization` == 6, and 2080 + 6 == 2086
    - `SELECT COUNT(*) FROM dim_division` == 21 with unique division_key; `dim_date` == 4383 (gapless); `dim_time` == 96
    - fact_vehicle has owner_division_key non-null on all rows and using_division_key NULL where Utilization is NULL
    - `SELECT COUNT(*) - COUNT(AVAILABILITY_YTD) FROM fact_vehicle` == 209
    - `model.py` contains no `INNER JOIN`, no `WHERE unit_key_int IS NOT NULL`, no `COALESCE`/`fillna`
    - `uv run pytest -q tests/test_join_integrity.py tests/test_dimensions.py` exits 0
  </acceptance_criteria>
  <done>All five Gold tables build with the verified target counts; flagship join correct; null discipline preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Wire gold fixture + write test_join_integrity.py + test_dimensions.py (MODEL-03/MODEL-02 gates)</name>
  <files>tests/conftest.py, tests/test_join_integrity.py, tests/test_dimensions.py</files>
  <read_first>
    - tests/conftest.py (the gold fixture from Plan 01 — add the model.build_all line)
    - tests/test_nulls.py (full file — the `_counts` helper + reconciliation assert idiom to mirror in test_join_integrity.py)
    - tests/test_rowcounts.py (full file — the count/parametrize idiom to mirror in test_dimensions.py)
    - .planning/phases/02-transform-model-join-integrity/02-PATTERNS.md § "test_join_integrity.py" (lines 196-231) and § "test_dimensions.py" (lines 261-270)
    - .planning/phases/02-transform-model-join-integrity/02-VALIDATION.md (Per-Task Verification Map, lines 42-58)
  </read_first>
  <action>
    Extend the `gold` fixture in `tests/conftest.py` to call `model.build_all(con)` immediately after `transform.build_all(con)` (the one-line change Plan 01 was structured for).
    Create `tests/test_join_integrity.py` (MODEL-03 hard gate) taking the `gold` fixture, mirroring test_nulls.py's count-helper + reconciliation style. Implement these exact node IDs (used verbatim by 02-VALIDATION.md):
    - `test_matched_2080` — `COUNT(*) FROM fact_vehicle WHERE Utilization IS NOT NULL` == 2080.
    - `test_unmatched_6` — `COUNT(*) FROM dq_unmatched_utilization` == 6 (and assert 2080 + 6 == 2086 reconciliation).
    - `test_fact_rowcount_4614` — fact_vehicle == 4614 (no fan-out; the 44 alnum units survived).
    - `test_fact_unique_key` — `COUNT(*) == COUNT(DISTINCT UNIT_NO)` == 4614.
    - `test_util_key_not_null` — `COUNT(*) FILTER (WHERE unit_key_int IS NULL) FROM stg_utilization` == 0.
    Create `tests/test_dimensions.py` (MODEL-02) taking `gold`:
    - `test_dim_date_gapless` — `COUNT(*)` == 4383 AND `datediff('day', MIN(date_key), MAX(date_key)) + 1 == COUNT(*)`.
    - `test_dim_time_96` — dim_time == 96.
    - `test_dim_division_conformed` — dim_division == 21 AND `COUNT(*) == COUNT(DISTINCT division_key)`.
    Use aliases `null_ct`/`in_both` — never reserved words `both`/`nulls` (Pitfall 6).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_join_integrity.py tests/test_dimensions.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` gold fixture calls `model.build_all` (greppable: `model\.build_all`)
    - `uv run pytest tests/test_join_integrity.py::test_matched_2080 -x` exits 0
    - `uv run pytest tests/test_join_integrity.py::test_unmatched_6 -x` exits 0
    - `uv run pytest tests/test_join_integrity.py::test_fact_rowcount_4614 -x` exits 0
    - `uv run pytest tests/test_join_integrity.py::test_fact_unique_key -x` exits 0
    - `uv run pytest tests/test_join_integrity.py::test_util_key_not_null -x` exits 0
    - `uv run pytest tests/test_dimensions.py::test_dim_date_gapless -x`, `::test_dim_time_96 -x`, `::test_dim_division_conformed -x` each exit 0
    - `uv run pytest -q` full suite green (Phase-1 + Plan-01 + Plan-02 tests)
  </acceptance_criteria>
  <done>MODEL-03 join-integrity gate and MODEL-02 dimension tests all green; gold fixture builds the full Gold layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stg_* tables → Gold tables | All inputs are internal DuckDB tables derived from trusted Phase-1 CSVs; no external/untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | model.py SQL (join + dims) | mitigate | Table/column names are internal literals; no external value interpolated into SQL. |
| T-02-04 | Tampering/Repudiation | join correctness + null discipline | mitigate | The join-integrity hard gate (matched/unmatched/fan-out/unique-key) plus the 209-null preservation guard are the integrity controls; the full pytest suite is the audit trail. |

No authentication, session, network, secret, or untrusted-input surface. No high-severity threats.
</threat_model>

<verification>
- `uv run pytest -q tests/test_join_integrity.py tests/test_dimensions.py` green (MODEL-02 + MODEL-03)
- `uv run pytest -q` full suite green (all phases)
- model.build_all produces the five Gold tables at verified counts (21 / 4614 / 272529 / 4383 / 96) with matched 2080 / unmatched 6
</verification>

<success_criteria>
- MODEL-03 satisfied: matched==2,080, unmatched==6, fact_vehicle==4,614 no fan-out, unique key, no NULL utilization join key — all tested
- MODEL-02 satisfied: dim_division conformed (21), fact_vehicle, fact_ferry, gapless dim_date (4,383), dim_time (96)
- 6 unmatched rows captured in dq_unmatched_utilization for the Plan-03 DQ finding
</success_criteria>

<output>
Create `.planning/phases/02-transform-model-join-integrity/02-02-SUMMARY.md` when done
</output>
