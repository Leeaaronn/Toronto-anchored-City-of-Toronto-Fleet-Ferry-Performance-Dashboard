---
phase: 02-transform-model-join-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fleet_analytics/config.py
  - src/fleet_analytics/transform.py
  - tests/conftest.py
  - tests/test_derived_fields.py
autonomous: true
requirements: [MODEL-01]
user_setup: []

must_haves:
  truths:
    - "UNIT_NO is normalized to a canonical integer join key on BOTH datasets, and the 44 alphanumeric availability units survive (not dropped)"
    - "Ferry timestamps are rounded to 15-minute slots with zero NaT and all 272,529 rows preserved"
    - "All six vehicle/ferry derived fields exist: fleet_age, season, daypart, day_of_week, is_weekend, sales_redemption_gap"
    - "REFERENCE_YEAR = 2023 drives fleet_age and is a documented config constant (D-08)"
    - "The gold pytest fixture builds the staging layer on the session con without re-ingesting Bronze"
  artifacts:
    - path: "src/fleet_analytics/config.py"
      provides: "REFERENCE_YEAR=2023, GOLD_DIR, GOLD_TABLES, GOLD_EXPECTED_ROWS constants"
      contains: "REFERENCE_YEAR"
    - path: "src/fleet_analytics/transform.py"
      provides: "build_keyed_availability / build_keyed_utilization / build_staged_ferry private builders + build_all(con) orchestrator"
      exports: ["build_all"]
      min_lines: 60
    - path: "tests/conftest.py"
      provides: "session-scoped gold fixture depending on con"
      contains: "def gold"
    - path: "tests/test_derived_fields.py"
      provides: "parametrized MODEL-01 derived-field + key-normalization + ferry-15min tests"
      min_lines: 40
  key_links:
    - from: "src/fleet_analytics/transform.py"
      to: "config.REFERENCE_YEAR"
      via: "parameter-bound ? in fleet_age SELECT"
      pattern: "REFERENCE_YEAR"
    - from: "tests/conftest.py gold fixture"
      to: "transform.build_all"
      via: "fixture body call"
      pattern: "transform\\.build_all"
---

<objective>
Build the Phase-2 transform (staging) layer: canonical-integer `UNIT_NO` key normalization on both datasets, ferry 15-minute slotting, and all six derived fields (`fleet_age`, `season`, `daypart`, `day_of_week`, `is_weekend`, `sales_redemption_gap`) — satisfying MODEL-01. Also lands the Wave 0 test scaffolding (`gold` fixture + `test_derived_fields.py`) so downstream model/export plans inherit a working feedback gate.

Purpose: MODEL-01 produces the keyed/derived staging tables that the Wave-2 Gold join and dimensions consume. The `TRY_CAST` key strategy is the non-obvious correctness pivot for the whole phase.
Output: `config.py` Gold constants, `transform.py`, extended `conftest.py` with `gold` fixture, `test_derived_fields.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/02-transform-model-join-integrity/02-CONTEXT.md
@.planning/phases/02-transform-model-join-integrity/02-RESEARCH.md
@.planning/phases/02-transform-model-join-integrity/02-PATTERNS.md
@.planning/phases/02-transform-model-join-integrity/02-VALIDATION.md

<interfaces>
<!-- Phase-1 contracts the executor builds against. No codebase exploration needed. -->

From src/fleet_analytics/config.py (extend, do not retype existing constants):
```python
PROJECT_ROOT: Path  # = Path(__file__).resolve().parents[2]
DATA_DIR: Path
def csv_path(filename: str) -> str
EXPECTED_ROWS: dict[str, int]   # {"bronze_availability":4614,"bronze_utilization":2086,"bronze_ferry":272529}
AVAIL_TYPES / UTIL_TYPES / FERRY_TYPES: dict[str, str]
# UNIT_NO is VARCHAR in Bronze (zero-padding preserved) -> Phase 2 normalizes the integer key here.
# AVAILABILITY_YTD is DOUBLE with 209 genuine NULLs -> NEVER COALESCE/fillna.
```

From src/fleet_analytics/ingest.py (the module-shape analog to mirror):
```python
def ingest_bronze(con: duckdb.DuckDBPyConnection) -> duckdb.DuckDBPyConnection
# private _create_bronze builders run one CREATE OR REPLACE TABLE AS SELECT, bind ? params,
# then a fail-fast count loop over EXPECTED_ROWS; returns con for chaining.
```

From tests/conftest.py (the fixture to extend):
```python
@pytest.fixture(scope="session")
def con() -> duckdb.DuckDBPyConnection   # :memory:, Bronze ingested once, yields, closes
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Gold-layer constants to config.py (D-08)</name>
  <files>src/fleet_analytics/config.py</files>
  <read_first>
    - src/fleet_analytics/config.py (full file — see the existing PROJECT_ROOT/DATA_DIR/EXPECTED_ROWS/csv_path single-source-of-truth pattern at lines 20-43; extend it, never inline values elsewhere)
    - .planning/phases/02-transform-model-join-integrity/02-PATTERNS.md § "config.py (MODIFY)" (lines 122-142 — exact additions)
    - .planning/phases/02-transform-model-join-integrity/02-CONTEXT.md D-08 (REFERENCE_YEAR rationale)
  </read_first>
  <action>
    Extend `src/fleet_analytics/config.py` (do not retype existing constants). Add, mirroring the existing constant style:
    - `REFERENCE_YEAR: int = 2023` with a docstring/comment citing the May 2023 FSD General Government Committee report and the audit 2022-2023 "actual" benchmark context (D-08 requires documenting the chosen value + rationale).
    - `GOLD_DIR: Path = PROJECT_ROOT / "data" / "gold"` (REQUIREMENTS SHIP-01 target output dir).
    - `GOLD_TABLES: list[str] = ["dim_division", "fact_vehicle", "fact_ferry", "dim_date", "dim_time"]`.
    - `GOLD_EXPECTED_ROWS: dict[str, int] = {"dim_division": 21, "fact_vehicle": 4614, "fact_ferry": 272529, "dim_date": 4383, "dim_time": 96}` (parallel to EXPECTED_ROWS; consumed by model.py fail-fast loop and test_dimensions.py in Plan 02).
    Do NOT modify the AVAILABILITY_YTD DOUBLE type or any existing type map.
  </action>
  <verify>
    <automated>uv run python -c "from fleet_analytics import config; assert config.REFERENCE_YEAR==2023; assert config.GOLD_TABLES==['dim_division','fact_vehicle','fact_ferry','dim_date','dim_time']; assert config.GOLD_EXPECTED_ROWS['dim_time']==96; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `config.py` contains `REFERENCE_YEAR = 2023` with a comment citing the 2023 FSD/audit benchmark rationale
    - `config.GOLD_DIR` resolves to `<PROJECT_ROOT>/data/gold`
    - `config.GOLD_TABLES` equals the five table names in the listed order
    - `config.GOLD_EXPECTED_ROWS` maps dim_division→21, fact_vehicle→4614, fact_ferry→272529, dim_date→4383, dim_time→96
    - The import-and-assert command above exits 0
  </acceptance_criteria>
  <done>All Gold constants present and importable; no existing Bronze constant altered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create transform.py — keyed + derived staging layer (MODEL-01)</name>
  <files>src/fleet_analytics/transform.py</files>
  <read_first>
    - src/fleet_analytics/ingest.py (full file — mirror the module docstring, `from __future__ import annotations` / `import duckdb` / `from fleet_analytics import config` imports, the private per-table `CREATE OR REPLACE TABLE AS SELECT` builder shape, and the `build_all`-style orchestrator that returns con)
    - .planning/phases/02-transform-model-join-integrity/02-RESEARCH.md Example 1 (lines 282-298, TRY_CAST key), Example 3 (lines 325-332, time_bucket), Example 4 (lines 334-350, season/daypart/dow/is_weekend/gap CASE block)
    - .planning/phases/02-transform-model-join-integrity/02-RESEARCH.md Pitfalls 1, 4, 5, 6 (lines 248-276)
    - .planning/phases/02-transform-model-join-integrity/02-PATTERNS.md § "transform.py" (lines 31-76)
    - .planning/phases/02-transform-model-join-integrity/02-CONTEXT.md D-07..D-12
  </read_first>
  <behavior>
    - stg_availability has `unit_key_int = TRY_CAST(UNIT_NO AS BIGINT)`; the 44 alphanumeric units (e.g. 296011A, CLAW10) yield NULL key but the row survives → 4,614 rows preserved
    - stg_availability has `fleet_age = REFERENCE_YEAR - YEAR` (bound via ?), range −3…41 (negatives NOT clamped)
    - stg_utilization has `unit_key_int = TRY_CAST(UNIT_NO AS BIGINT)` with zero NULL keys (utilization UNIT_NO is 100% numeric)
    - stg_ferry has `ts_15 = time_bucket(INTERVAL '15 minutes', "Timestamp")` with 0 NaT and 272,529 rows
    - stg_ferry has season (D-09), daypart (D-10), day_of_week + is_weekend (D-12), sales_redemption_gap = "Sales Count" − "Redemption Count" signed (D-11)
    - AVAILABILITY_YTD nullability is untouched (no COALESCE/fillna) → 209 NULLs flow through
  </behavior>
  <action>
    Create `src/fleet_analytics/transform.py` mirroring `ingest.py`'s module shape. Open with a docstring naming MODEL-01 and the load-bearing columns and the D-07..D-12 rationale. Define three private builders, each running ONE `CREATE OR REPLACE TABLE … AS SELECT … FROM bronze_*`:
    - `build_keyed_availability(con)` → `stg_availability`: `SELECT *, TRY_CAST(UNIT_NO AS BIGINT) AS unit_key_int, (? - YEAR) AS fleet_age` binding `config.REFERENCE_YEAR` as the `?` param (D-07/D-08). Embed the SQL body from 02-RESEARCH.md Example 1. Do NOT use plain `CAST` (throws on the 44 alphanumeric units — Pitfall 1); do NOT clamp negative fleet_age (Pitfall 5).
    - `build_keyed_utilization(con)` → `stg_utilization`: `SELECT *, TRY_CAST(UNIT_NO AS BIGINT) AS unit_key_int`.
    - `build_staged_ferry(con)` → `stg_ferry`: add `ts_15 = time_bucket(INTERVAL '15 minutes', "Timestamp")` (Example 3) plus the season/daypart/day_of_week/is_weekend/sales_redemption_gap CASE/scalar block from Example 4. Use scalar `month()/hour()/isodow()/dayname()` or single-quoted `date_part('month', …)` — NEVER double-quoted `date_part("month", …)` (Binder Error, Pitfall 4). `sales_redemption_gap` is signed (D-11) — do NOT use abs().
    Add `build_all(con: duckdb.DuckDBPyConnection) -> duckdb.DuckDBPyConnection` that calls the three builders and returns `con` (consumed by the `gold` fixture and Plan 02's `model.build_all`). No fenced code in this action — reuse the verified SQL bodies from the cited RESEARCH examples.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_derived_fields.py</automated>
  </verify>
  <acceptance_criteria>
    - `transform.build_all(con)` runs on a Bronze-ingested con without error and returns con
    - `SELECT COUNT(*) FROM stg_availability` == 4614 (the 44 alphanumeric units survived with NULL unit_key_int)
    - `SELECT COUNT(*) FILTER (WHERE unit_key_int IS NULL) FROM stg_utilization` == 0
    - `SELECT COUNT(*) FROM stg_ferry` == 272529 and `COUNT(*) FILTER (WHERE ts_15 IS NULL)` == 0
    - stg_ferry exposes columns season, daypart, day_of_week, is_weekend, sales_redemption_gap
    - stg_availability exposes fleet_age and unit_key_int; AVAILABILITY_YTD still has 209 NULLs
    - `transform.py` contains no `COALESCE`/`fillna`/`CAST(UNIT_NO AS BIGINT)` (non-TRY) and no double-quoted `date_part("`
    - `uv run pytest -q tests/test_derived_fields.py` exits 0
  </acceptance_criteria>
  <done>Staging layer builds correctly; all MODEL-01 derived fields present; null discipline preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 — gold fixture + test_derived_fields.py (MODEL-01 gate)</name>
  <files>tests/conftest.py, tests/test_derived_fields.py</files>
  <read_first>
    - tests/conftest.py (full file — the existing session-scoped `con` fixture at lines 14-19; extend, do not rewrite)
    - tests/test_rowcounts.py (full file — the `@pytest.mark.parametrize` + `con.execute(...).fetchone()[0]` assertion idiom to copy)
    - tests/test_nulls.py (full file — the private `_counts` helper + reconciliation assert idiom)
    - .planning/phases/02-transform-model-join-integrity/02-PATTERNS.md § "conftest.py" (lines 170-192) and § "test_derived_fields.py" (lines 235-257)
    - .planning/phases/02-transform-model-join-integrity/02-VALIDATION.md (boundary test cases, lines 62-66)
  </read_first>
  <action>
    Extend `tests/conftest.py`: add a session-scoped fixture `gold(con)` that imports `transform` (and later `model`) and calls `transform.build_all(con)`, returning `con`. For THIS plan it may call only `transform.build_all(con)`; Plan 02 will extend the same fixture to also call `model.build_all(con)` — write it so adding that one line is the only change (import both modules; call transform now, model added in Plan 02). Mirror the existing `con` fixture style.
    Create `tests/test_derived_fields.py` mirroring `test_rowcounts.py`'s parametrize idiom and taking the `gold` fixture. Implement these node IDs exactly (used verbatim by 02-VALIDATION.md):
    - `test_ferry_ts15` — stg_ferry rowcount == 272529 AND `COUNT(*) FILTER (WHERE ts_15 IS NULL)` == 0.
    - `test_fleet_age[...]` — parametrized: YEAR 2015→8, 1982→41, 2026→−3 (assert negatives allowed; query stg_availability or assert `REFERENCE_YEAR - YEAR` per case).
    - `test_season_daypart[...]` — parametrized season (month 12→Winter, 2→Winter, 3→Spring, 6→Summer, 8→Summer, 9→Fall, 11→Fall) and daypart (hour 5→Night, 6→Morning, 10→Morning, 11→Midday, 14→Midday, 15→Afternoon/Evening, 19→Afternoon/Evening, 20→Night, 23→Night) against the same CASE logic transform.py uses.
    - `test_gap_signed` — assert sales_redemption_gap == "Sales Count" − "Redemption Count" (signed) for a known stg_ferry row; assert at least one negative gap exists (proves no abs()).
    Use aliases like `null_ct`/`in_both` — never reserved words `both`/`nulls` (Pitfall 6).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_derived_fields.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` defines `def gold(con)` calling `transform.build_all` (greppable: `transform\.build_all`)
    - `uv run pytest tests/test_derived_fields.py::test_ferry_ts15 -x` exits 0
    - `uv run pytest "tests/test_derived_fields.py::test_fleet_age" -x` exits 0 and includes the 2026→−3 negative case
    - `uv run pytest "tests/test_derived_fields.py::test_season_daypart" -x` exits 0 across all listed boundary cases
    - `uv run pytest tests/test_derived_fields.py::test_gap_signed -x` exits 0
    - `uv run pytest -q` (full suite) stays green including the existing Phase-1 `test_nulls.py` 209/4,405 guard
  </acceptance_criteria>
  <done>gold fixture builds the staging layer; all MODEL-01 derived-field tests green; Phase-1 regression guards unbroken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV file → DuckDB | Three trusted City-of-Toronto CSVs already validated in Phase 1; no untrusted/external input crosses into this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | transform.py SQL construction | mitigate | Bind values (REFERENCE_YEAR) with `?`; never f-string-interpolate external values into SQL — reuse the Phase-1 ingest.py `?`-binding pattern. Table/column names are internal literals. |
| T-02-02 | Tampering/Repudiation | AVAILABILITY_YTD null discipline | mitigate | No COALESCE/fillna anywhere; the existing test_nulls.py 209/4,405 guard plus the full suite is the integrity audit trail. |

No authentication, session, network, secret-handling, or untrusted-input surface exists in this phase (local read-only CSV → in-memory DuckDB). No high-severity threats.
</threat_model>

<verification>
- `uv run pytest -q tests/test_derived_fields.py` green (MODEL-01 staging + derived fields)
- `uv run pytest -q` full suite green (Phase-1 regression guards including test_nulls.py intact)
- `transform.build_all(con)` produces stg_availability (4614), stg_utilization (0 NULL keys), stg_ferry (272529, 0 NaT)
</verification>

<success_criteria>
- MODEL-01 satisfied: canonical-integer key on both datasets (44 alnum units survive), ferry 15-min slots (0 NaT / 272,529), all six derived fields produced
- REFERENCE_YEAR=2023 documented in config.py (D-08)
- gold fixture + test_derived_fields.py exist and gate MODEL-01
</success_criteria>

<output>
Create `.planning/phases/02-transform-model-join-integrity/02-01-SUMMARY.md` when done
</output>
