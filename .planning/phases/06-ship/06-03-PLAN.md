---
phase: 06-ship
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - README.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [SHIP-01]
must_haves:
  truths:
    - "A README documents the one-command run, citations, stated assumptions, pull date, and a dated test result"
    - "The three primary sources are cited under the Open Government Licence – Toronto"
    - "Headline numbers in the README are consistent with deliverables and kpi_values.json (no conflation of the two 5.8% bases)"
    - "The REQUIREMENTS.md traceability table reflects actually-complete work (no stale Pending checkboxes for shipped requirements)"
    - "All three required deliverables (modeled data + specs, report spec, two narratives) are confirmed present and self-consistent"
  artifacts:
    - path: "README.md"
      provides: "Top-level submission documentation: run instructions, citations, assumptions, test gate"
      contains: "Open Government Licence"
      min_lines: 40
    - path: ".planning/REQUIREMENTS.md"
      provides: "Reconciled traceability table matching completed work"
      contains: "SHIP-01"
  key_links:
    - from: "README.md"
      to: "data/kpi/kpi_values.json and deliverables/kpi_definitions.md"
      via: "links headline numbers to source of truth rather than restating the full set"
      pattern: "kpi_values\\.json|kpi_definitions"
    - from: "README.md"
      to: "uv run python -m fleet_analytics.pipeline"
      via: "documents the one-command run as the runnable invocation"
      pattern: "fleet_analytics\\.pipeline"
---

<objective>
Write the README that turns the finished work into a credible, cited, reproducible submission, reconcile the stale traceability table, and confirm all three required deliverables are complete and self-consistent.

Purpose: SHIP-01 Success Criteria 3 and 4. No README exists today — it is the main documentation gap. The README must AGGREGATE (never restate) the canonical numbers already present in the deliverables and `data/kpi/kpi_values.json`, cite the three primary sources under the Open Government Licence – Toronto, state the 2026-06-02 pull date and stated assumptions, and document the test gate as a DATED command result (not an eternal invariant). It must preserve — not "fix" — the two-denominator 5.8% nuance (120/2,086 raw vs 0.0572 over 2,080 matched). Separately, REQUIREMENTS.md shows KPI-01 and REPORT-01 as "Pending" while their artifacts exist and ROADMAP marks the phases Complete — a doc-drift reconcile that requires a human confirmation gate (RESEARCH A2) before flipping.

Output: `README.md` (new), reconciled `.planning/REQUIREMENTS.md`, and a confirmed self-consistency cross-check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ship/06-RESEARCH.md
@.planning/phases/06-ship/06-PATTERNS.md
@.planning/phases/06-01-SUMMARY.md
@.planning/phases/06-02-SUMMARY.md
@deliverables/data_dictionary.md
@data/kpi/kpi_values.json

<interfaces>
<!-- Canonical headline numbers — quote ONLY these in the README; source of truth is data/kpi/kpi_values.json -->
<!-- (RESEARCH lines 222-232 + Self-Consistency Cross-Check Map lines 235-247). -->
89.0% pooled availability (0.8899...) — distinct from mean-of-class-means (0.8786)
5.8% underutilization (0.0572) — PRESERVE the two-denominator nuance, do NOT conflate:
    • 120/2,086 = 5.75% raw light-duty (in data_dictionary.md / dq_report.md)
    • 0.0572 over 2,080 matched units (KPI layer)
2,080 matched / 6 unmatched (light-duty availability⋈utilization join integrity)
209 nulls excluded / 4,405 non-null availability denominator
Row counts: 4,614 (availability) / 2,086 (utilization) / 272,529 (ferry)

<!-- README header/attribution style to mirror (deliverables/data_dictionary.md lines 1-6): -->
<!-- # Title; bold **Project:** Fleet Services Analytics — City of Toronto BA Assignment; -->
<!-- a "generated/run from" pointer; a dated snapshot line; inline cross-links to companion docs. -->

<!-- Three primary sources (CLAUDE.md Constraints; already cited in deliverables): -->
1. City of Toronto Open Data portal
2. May 2023 FSD General Government Committee report
3. AG Operational Review 2019.AU2.2 / 2019.AU2.3
Licence: Open Government Licence – Toronto. Pull date: 2026-06-02.

<!-- Run instructions: uv sync → uv run python -m fleet_analytics.pipeline (now runnable after Plan 01+02). -->
<!-- Inputs: committed .planning/data/ ; optional FLEET_DATA_DIR override (config.py line 24). -->
<!-- Test gate stated as dated result: "uv run pytest -q → 77 passed, as of 2026-06-06" (Pitfall 4 — not eternal). -->

<!-- Scope boundary (CLAUDE.md): GSD owns data engineering; Power BI canvas authored manually — no .pbix/PBIP/TMDL. -->

<!-- REQUIREMENTS.md drift (RESEARCH Pitfall 5, lines 203-205): KPI-01 (Phase 3) and REPORT-01 (Phase 4) -->
<!-- show "Pending"/[ ] while artifacts exist (data/kpi/, deliverables/report_spec.md) and ROADMAP marks -->
<!-- Phases 3-4 Complete. A2 caveat: confirm with the user before flipping (checkpoint Task 3). -->

<!-- Three required deliverables to confirm self-consistent (SHIP-01 criterion 4): -->
<!--   (a) modeled data + specs: data/gold/*.parquet + deliverables/kpi_definitions.md + measures_spec.md -->
<!--   (b) report spec: deliverables/report_spec.md -->
<!--   (c) two narratives: deliverables/requirements_approach.md + stakeholder_engagement.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md aggregating run, citations, assumptions, and the dated test gate</name>
  <files>README.md</files>
  <read_first>
    - deliverables/data_dictionary.md (lines 1-6 — header/attribution style to mirror)
    - data/kpi/kpi_values.json (the canonical headline numbers — quote, do not recompute)
    - CLAUDE.md (Project + Constraints + Sourcing sections — scope boundary, three sources, licence)
    - src/fleet_analytics/config.py (line 24 — FLEET_DATA_DIR optional override; line 28 — the trailing-space filename, do NOT propose "fixing" it)
    - .planning/phases/06-ship/06-RESEARCH.md (Pattern 2 lines 145-148, Anti-Patterns lines 149-153, Self-Consistency Cross-Check Map lines 235-247, Pitfall 4 lines 199-201)
    - .planning/phases/06-ship/06-PATTERNS.md (README section lines 144-169)
  </read_first>
  <action>
    Create `README.md` at repo root, min ~40 lines. Mirror the deliverable header style: `# Fleet Services Analytics — City of Toronto BA Assignment`, a bold `**Project:**` line, a "generated/run from" pointer, and the `**Snapshot pull date:** 2026-06-02` line with inline cross-links to companion deliverables. Include these sections: (1) What this is / scope boundary — state GSD owns the data engineering layer only; the Power BI report canvas is authored manually; NO `.pbix`/PBIP/TMDL is produced. (2) How to run — `uv sync` then `uv run python -m fleet_analytics.pipeline` (now runnable post-Plan-01/02); note the committed `.planning/data/` inputs and the optional `FLEET_DATA_DIR` override. (3) Test gate — state the command `uv run pytest -q` and a DATED result ("77 passed, as of 2026-06-06" — confirm the actual current count by running it; frame as "as of <date>", not an eternal invariant — Pitfall 4). (4) Outputs / handoff — `data/gold/*.parquet` (6 tables) for Power BI import + `data/kpi/` snapshot + `deliverables/`. (5) Headline numbers — quote ONLY the five headline figures (89.0% pooled availability distinct from 0.8786 mean-of-class-means; 5.8% underutilization; 2,080 matched / 6 unmatched; 209 nulls excluded / 4,405 denom; row counts 4,614/2,086/272,529) and LINK to `deliverables/kpi_definitions.md` and `data/kpi/kpi_values.json` as source of truth rather than restating the full KPI set. PRESERVE the two-denominator 5.8% nuance — explicitly note the raw 120/2,086 vs the over-2,080-matched 0.0572; do NOT conflate them. (6) Stated assumptions — 209 nulls excluded not imputed; underutilization pre-classified not recomputed; thresholds cited from audit not recalculated; sales-vs-redemption is an assumption flagged for SME validation; the 5.8% vs ~14% AG framing. (7) Sources & licence — cite the three primary sources (City of Toronto Open Data portal; May 2023 FSD General Government Committee report; AG Operational Review 2019.AU2.2 / 2019.AU2.3) under the Open Government Licence – Toronto. Do NOT restate every KPI value (Anti-Pattern); do NOT hardcode a number that will drift; do NOT propose "fixing" the trailing-space CSV filename.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib,sys; t=pathlib.Path('README.md').read_text(encoding='utf-8'); req=['Open Government Licence','2026-06-02','fleet_analytics.pipeline','uv run pytest','2019.AU2','FLEET_DATA_DIR']; missing=[x for x in req if x not in t]; sys.exit('MISSING: '+', '.join(missing) if missing else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` exists at repo root, ≥40 lines
    - Contains the exact one-command invocation string `uv run python -m fleet_analytics.pipeline`
    - States `uv run pytest -q` as the test gate with a dated result ("as of 2026-06-06") — count verified by actually running it, not guessed
    - Cites all three primary sources: City of Toronto Open Data, May 2023 FSD report, AG 2019.AU2.2/2.3
    - Contains the string "Open Government Licence" and the pull date "2026-06-02"
    - Mentions the optional `FLEET_DATA_DIR` override
    - States the scope boundary (no .pbix/PBIP/TMDL; Power BI canvas authored manually)
    - The two 5.8% bases are presented distinctly (120/2,086 raw AND 0.0572 over 2,080 matched) — not conflated
    - Links to `deliverables/kpi_definitions.md` and/or `data/kpi/kpi_values.json` rather than restating the full KPI set
    - Stated-assumptions section present (209 nulls excluded, underutilization pre-classified, thresholds cited, sales-vs-redemption assumption)
  </acceptance_criteria>
  <done>README.md documents the runnable one-command pipeline, the dated test gate, the three cited sources under the OGL-Toronto, the pull date, stated assumptions, and headline numbers linked to (not duplicating) the source of truth.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-check README and deliverable number self-consistency</name>
  <files>README.md</files>
  <read_first>
    - README.md (from Task 1)
    - data/kpi/kpi_values.json (canonical values)
    - deliverables/kpi_definitions.md, deliverables/measures_spec.md, deliverables/report_spec.md, deliverables/dq_report.md, deliverables/data_dictionary.md, deliverables/requirements_approach.md, deliverables/stakeholder_engagement.md
    - .planning/phases/06-ship/06-RESEARCH.md (Self-Consistency Cross-Check Map lines 235-247)
    - .planning/phases/06-ship/06-VALIDATION.md (Manual-Only Verifications lines 64-67)
  </read_first>
  <action>
    Grep each registry number — 89.0% / 0.8899, 5.8% / 0.0572, 2,080, 6 (unmatched), 209, 4,405, 4,614, 2,086, 272,529 — across `deliverables/` and the new `README.md`, and confirm they agree with `data/kpi/kpi_values.json` (RESEARCH Cross-Check Map, all five rows confirmed consistent this session). Confirm the README does NOT conflate the two 5.8% bases. Confirm the three required deliverables are present and internally consistent: (a) modeled data + specs (`data/gold/*.parquet` 6 files + `deliverables/kpi_definitions.md` + `deliverables/measures_spec.md`), (b) report spec (`deliverables/report_spec.md`), (c) two narratives (`deliverables/requirements_approach.md` + `deliverables/stakeholder_engagement.md`). If any number in the README disagrees with `kpi_values.json` or a deliverable, fix the README (the deliverables and snapshot are the source of truth — do not edit them to match the README). Record the cross-check result for the SUMMARY.
  </action>
  <verify>
    <automated>uv run python -c "import json,pathlib,sys; v=json.loads(pathlib.Path('data/kpi/kpi_values.json').read_text()); files=['README.md','deliverables/kpi_definitions.md','deliverables/report_spec.md','deliverables/requirements_approach.md','deliverables/stakeholder_engagement.md']; missing=[f for f in files if not pathlib.Path(f).exists()]; sys.exit('MISSING FILES: '+', '.join(missing) if missing else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - All five cross-check rows (89.0% pooled; 5.8% underutil; 2,080/6; 209/4,405; row counts) appear consistently in README and at least one deliverable, matching kpi_values.json
    - README does not conflate the two 5.8% denominators
    - All three required deliverable groups confirmed present: modeled data+specs, report spec, two narratives (verify command confirms the key files exist)
    - Any README/deliverable mismatch was resolved by editing the README, never the deliverables or the snapshot
  </acceptance_criteria>
  <done>README headline numbers verified consistent with deliverables and kpi_values.json; all three required deliverable groups confirmed present and self-consistent.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Confirm KPI-01 / REPORT-01 completion before reconciling the traceability table</name>
  <decision>Are requirements KPI-01 (Phase 3) and REPORT-01 (Phase 4) genuinely complete, so the REQUIREMENTS.md traceability table can be flipped from "Pending"/[ ] to "Complete"/[x]?</decision>
  <context>
    RESEARCH Pitfall 5 / Assumption A2 (Medium risk): `.planning/REQUIREMENTS.md` shows KPI-01 and REPORT-01 as "Pending" (`[ ]`) while ROADMAP marks Phases 3 and 4 "Complete", STATE shows 15/15 plans complete, and the artifacts exist (`data/kpi/` KPI tables, `deliverables/report_spec.md`). This is almost certainly stale checkbox drift, but flipping a table to "Complete" could hide a genuine gap if the work is actually unfinished — so the planner requires explicit confirmation before the auto reconcile in Task 4. The artifacts have been verified present in Task 2.
  </context>
  <options>
    <option id="reconcile">
      <name>Reconcile (flip to Complete)</name>
      <pros>Removes doc drift; REQUIREMENTS.md becomes self-consistent with ROADMAP/STATE; satisfies SHIP-01 "all deliverables confirmed self-consistent". Artifacts confirmed present in Task 2.</pros>
      <cons>If either requirement is secretly unfinished, this masks it (deemed very unlikely — artifacts exist and phases are marked complete).</cons>
    </option>
    <option id="leave">
      <name>Leave as-is (document the discrepancy instead)</name>
      <pros>Zero risk of masking an unfinished requirement.</pros>
      <cons>Leaves a visible inconsistency between REQUIREMENTS.md and ROADMAP/STATE that contradicts SHIP-01 criterion 4.</cons>
    </option>
  </options>
  <resume-signal>Select: reconcile or leave</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Reconcile the REQUIREMENTS.md traceability table per the Task 3 decision</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 26-40 the requirement checkboxes; lines 68-83 the traceability table)
    - .planning/ROADMAP.md (lines 17-20 — Phases 3/4/5 marked Complete, source of truth for status)
    - .planning/phases/06-ship/06-RESEARCH.md (Pitfall 5 lines 203-205)
    - .planning/phases/06-ship/06-PATTERNS.md (REQUIREMENTS.md section lines 200-204)
  </read_first>
  <action>
    Apply the Task 3 decision. If "reconcile": update `.planning/REQUIREMENTS.md` so KPI-01 and REPORT-01 read `[x]` in the v1 requirements checklist (lines 26/31) and "Complete" in the traceability table (lines 78/80), matching ROADMAP/STATE — these are the only drifted rows; NARR-01/NARR-02 are Phase 5 (now complete) so reconcile those too if they still show "Pending" (ROADMAP line 19 marks Phase 5 Complete). Also mark SHIP-01 traceability status to "In Progress" or "Complete" only as accurate at write time (it becomes Complete at phase verification; leave as "Pending"/"In Progress" — do NOT pre-flip SHIP-01 to Complete before /gsd:verify-work). Do NOT touch DATA-*/MODEL-* rows (already Complete). Do NOT edit any number or requirement text — only status fields/checkboxes. If "leave": instead add a short note under the traceability table documenting that KPI-01/REPORT-01 show Pending pending confirmation, and do not flip checkboxes. Update the file's "Last updated" footer line to reflect this reconcile (dated 2026-06-06).
  </action>
  <verify>
    <automated>uv run python -c "import pathlib,sys; t=pathlib.Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'); sys.exit(0 if ('KPI-01' in t and 'REPORT-01' in t and 'SHIP-01' in t) else 'traceability rows missing')"</automated>
  </verify>
  <acceptance_criteria>
    - If decision was "reconcile": KPI-01 and REPORT-01 show `[x]` in the checklist AND "Complete" in the traceability table; NARR-01/NARR-02 also reconciled to match Phase 5 Complete
    - SHIP-01 is NOT pre-flipped to "Complete" (it completes at /gsd:verify-work)
    - DATA-*/MODEL-* rows are unchanged
    - No requirement text or any number was edited — only status fields/checkboxes
    - The "Last updated" footer reflects the 2026-06-06 reconcile
    - If decision was "leave": a documenting note is present and no checkbox was flipped
  </acceptance_criteria>
  <done>REQUIREMENTS.md traceability is reconciled per the human decision; the table is self-consistent with ROADMAP/STATE (or the discrepancy is explicitly documented), with SHIP-01 left for phase verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Human review of the README as submission-grade documentation</name>
  <what-built>
    A new `README.md` documenting the one-command pipeline run, the dated test gate, the three primary sources under the Open Government Licence – Toronto, the 2026-06-02 pull date, stated assumptions, and headline numbers linked to the source of truth; plus a reconciled REQUIREMENTS.md traceability table. The full suite is green and `uv run python -m fleet_analytics.pipeline` runs end-to-end with a clean no-op diff.
  </what-built>
  <how-to-verify>
    1. Open `README.md` and read it end-to-end. Confirm it reads as credible, panel-interview-grade submission documentation — not a stub.
    2. Confirm the three primary sources are cited (City of Toronto Open Data portal; May 2023 FSD General Government Committee report; AG Operational Review 2019.AU2.2 / 2019.AU2.3) under the Open Government Licence – Toronto, and the pull date 2026-06-02 is stated.
    3. Confirm the stated-assumptions section is present and accurate (209 nulls excluded not imputed; underutilization pre-classified; thresholds cited; sales-vs-redemption assumption; 5.8% vs ~14% AG framing).
    4. Confirm the two 5.8% bases are presented distinctly (120/2,086 raw vs 0.0572 over 2,080 matched) — not conflated.
    5. Optionally, from a clean shell, run `uv sync` then `uv run python -m fleet_analytics.pipeline` and confirm it exits 0, then `git status` shows no data-file churn.
  </how-to-verify>
  <resume-signal>Type "approved" or describe required edits</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → committed docs | README/REQUIREMENTS edits crossing into the shipped submission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Tampering | new code/SQL surface | accept | This plan authors only Markdown — no Python, no SQL, no interpolation surface. |
| T-6-03 | Repudiation / Integrity | README number drift vs source of truth | mitigate | README quotes only headline numbers and links to `kpi_values.json`/`kpi_definitions.md`; Task 2 grep-verifies consistency; the two 5.8% bases preserved distinctly (RESEARCH line 247). Test gate stated as a dated result, not an eternal invariant (Pitfall 4). |
| T-6-04 | Integrity | flipping a traceability checkbox masks unfinished work | mitigate | Reconcile gated behind a blocking human decision (Task 3) after artifacts confirmed present (Task 2); SHIP-01 not pre-flipped before verification. |
</threat_model>

<verification>
- README contains the runnable invocation, dated test gate, three cited sources under OGL-Toronto, pull date, stated assumptions, and the distinct 5.8% bases (automated string checks + human review)
- Headline numbers consistent across README, deliverables, and kpi_values.json
- All three required deliverable groups present (modeled data+specs, report spec, two narratives)
- REQUIREMENTS.md traceability reconciled per human decision; SHIP-01 left for /gsd:verify-work
- Human approves README as submission-grade
</verification>

<success_criteria>
- README documents citations, stated assumptions, pull date, dated test results, and the one-command run (SHIP-01 criterion 3)
- All three required deliverables confirmed complete and self-consistent (SHIP-01 criterion 4)
- Traceability drift resolved with human confirmation
- Submission is packaged, reproducible, cited, and self-consistent
</success_criteria>

<output>
Create `.planning/phases/06-ship/06-03-SUMMARY.md` when done.
</output>
