---
phase: 04-power-bi-report-specification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fleet_analytics/class_target.py
  - data/gold/dim_class_target.csv
  - data/gold/dim_class_target.parquet
  - tests/test_class_target.py
autonomous: true
requirements: [REPORT-01]

must_haves:
  truths:
    - "A committed dim_class_target reference table exists in data/gold/ as type-preserving Parquet + readable CSV"
    - "dim_class_target keys on UNIT_TYPE so it reproduces the validated Phase-3 per-class availability targets (joins fact_vehicle[UNIT_TYPE], NOT CATEGORY_CLASS)"
    - "A pytest guard asserts the exact 5 audit target values (Light 95 / Medium 92 / Heavy 85 / Off-Road 88 / Other 90) so the target line stays falsifiable"
    - "D-03: delivers the committed dim_class_target reference dimension (config-sourced audit targets, type-preserving Parquet+CSV, pytest-guarded), keyed on UNIT_TYPE per the join-key reconciliation"
  artifacts:
    - path: "src/fleet_analytics/class_target.py"
      provides: "build_class_target() builder + write_class_target() COPY exporter, config-sourced"
      contains: "_class_targets_relation"
    - path: "data/gold/dim_class_target.parquet"
      provides: "Type-preserving committed reference dimension (unit_type, class_label, target)"
    - path: "data/gold/dim_class_target.csv"
      provides: "Readable secondary CSV of the same table"
    - path: "tests/test_class_target.py"
      provides: "Row-count + exact-value guard, parametrized over the 5 class targets"
  key_links:
    - from: "src/fleet_analytics/class_target.py"
      to: "src/fleet_analytics/config.py"
      via: "ASSET_CLASS_TARGETS + UNIT_TYPE_TO_CLASS bound into a VALUES relation"
      pattern: "config\\.(ASSET_CLASS_TARGETS|UNIT_TYPE_TO_CLASS)"
    - from: "tests/test_class_target.py"
      to: "data/gold/dim_class_target.parquet"
      via: "second :memory: connection read_parquet in try/finally"
      pattern: "read_parquet"
---

<objective>
Build the committed `dim_class_target` reference dimension — the small audit-target table the Power BI report joins to `fact_vehicle` so the availability-by-class target line is reproducible and falsifiable — and guard it with a pytest. This plan also RESOLVES the high-priority join-key discrepancy flagged in 04-PATTERNS.md: `dim_class_target` keys on `UNIT_TYPE` (the validated Phase-3 key), NOT on `CATEGORY_CLASS` (which holds 19 granular codes, not the 5 audit labels).

Purpose: The report spec (Plans 02/03) references this table's relationship and its 5 target values. Locking the table + key in code first means the spec maps onto a real, tested artifact rather than a placeholder.
Output: `src/fleet_analytics/class_target.py`, `data/gold/dim_class_target.{parquet,csv}`, `tests/test_class_target.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-power-bi-report-specification/04-CONTEXT.md
@.planning/phases/04-power-bi-report-specification/04-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase — the executor should use these directly, no exploration needed. -->

From src/fleet_analytics/config.py (the ONLY source of the target values — never recalculate):
```python
GOLD_DIR: Path = PROJECT_ROOT / "data" / "gold"
ASSET_CLASS_TARGETS: dict[str, int] = {
    "Light": 95, "Medium": 92, "Heavy": 85, "Off-Road": 88, "Other": 90,
}
UNIT_TYPE_TO_CLASS: dict[str, str] = {
    "LIGHT DUTY": "Light", "MEDIUM DUTY": "Medium", "HEAVY DUTY": "Heavy",
    "OFF-ROAD": "Off-Road", "OTHER": "Other",
}
```

From src/fleet_analytics/kpis.py (the canonical "config dict -> bound VALUES relation" idiom; join key = UNIT_TYPE):
```python
def _class_targets_relation() -> tuple[str, list[object]]:
    rows, params = [], []
    for unit_type, class_label in config.UNIT_TYPE_TO_CLASS.items():
        target = config.ASSET_CLASS_TARGETS[class_label]
        rows.append("(?, ?, ?)")
        params.extend([unit_type, class_label, target])
    values = ", ".join(rows)
    relation = f"(VALUES {values}) AS ct(unit_type, class_label, target)"
    return relation, params
# kpis.py joins targets with:  JOIN {relation} ON fv.UNIT_TYPE = ct.unit_type
```

From src/fleet_analytics/export.py (the COPY-to-Parquet+CSV idiom; only config values reach the SQL string):
```python
config.GOLD_DIR.mkdir(parents=True, exist_ok=True)
con.execute(f"COPY (SELECT * FROM {t}) TO '{p}.parquet' (FORMAT PARQUET)")
con.execute(f"COPY (SELECT * FROM {t}) TO '{p}.csv' (FORMAT CSV, HEADER)")
```

From tests/conftest.py (session-scoped fixtures; `con` = Bronze ingested, `gold` = transform+model+kpis built):
```python
@pytest.fixture(scope="session")
def con(...): ...      # Bronze only
@pytest.fixture(scope="session")
def gold(con): ...     # transform.build_all + model.build_all + kpis.build_all
```

From tests/test_dimensions.py (the exact dimension-guard idiom to mirror):
```python
def test_dim_division_conformed(gold) -> None:
    total, distinct = gold.execute(
        "SELECT COUNT(*), COUNT(DISTINCT division_key) FROM dim_division"
    ).fetchone()
    assert total == 21
    assert total == distinct
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build dim_class_target builder + COPY exporter (UNIT_TYPE bridge)</name>
  <read_first>
    - src/fleet_analytics/config.py (ASSET_CLASS_TARGETS, UNIT_TYPE_TO_CLASS, GOLD_DIR — the only source of values)
    - src/fleet_analytics/kpis.py lines 79-96 (the _class_targets_relation VALUES idiom — copy its shape)
    - src/fleet_analytics/export.py (the COPY-to-Parquet+CSV idiom + the "only config values reach SQL" security note)
  </read_first>
  <behavior>
    - dim_class_target has exactly 10 rows: one per UNIT_TYPE in config.UNIT_TYPE_TO_CLASS, columns (unit_type, class_label, target).
    - There are exactly 5 distinct class_label values and exactly 5 distinct target values.
    - target for Heavy == 85; target for Light == 95; target for Medium == 92; Off-Road == 88; Other == 90.
    - The table joins fact_vehicle on UNIT_TYPE (NOT CATEGORY_CLASS) — verify the 5 distinct class_label values match the audit labels.
  </behavior>
  <action>
    Create `src/fleet_analytics/class_target.py` with two functions. (1) `build_class_target(con)` — register the audit targets as a DuckDB table named `dim_class_target` by binding from config: reuse the exact `_class_targets_relation()` shape from kpis.py (a `(VALUES (?, ?, ?), ...) AS ct(unit_type, class_label, target)` fragment with `?`-bound params drawn from `config.UNIT_TYPE_TO_CLASS` + `config.ASSET_CLASS_TARGETS`), then `CREATE OR REPLACE TABLE dim_class_target AS SELECT ct.unit_type, ct.class_label, ct.target FROM {relation}`. NEVER inline the 5 values into the SQL string — bind them via `?` (matches the config-as-single-source-of-truth and the security note in export.py: only config constants reach SQL).

    GRAIN/KEY DECISION (resolves the 04-PATTERNS.md discrepancy): the committed table is a 10-row bridge keyed on `unit_type`, NOT a 5-row `category_class` dim. Rationale to put in the module docstring: `fact_vehicle.CATEGORY_CLASS` holds 19 granular codes (CLASS1-8, APPARATUS, ...), while the 5 audit labels live one level up via `UNIT_TYPE` (LIGHT DUTY/MEDIUM DUTY/HEAVY DUTY/OFF-ROAD/OTHER). kpis.py joins targets on `fv.UNIT_TYPE = ct.unit_type`, so the Power BI relationship must too. `dim_class_target[unit_type] -> fact_vehicle[UNIT_TYPE]` reproduces the validated by-class values; a naive `CATEGORY_CLASS` relationship would not (19 vs 5 cardinality mismatch).

    (2) `write_class_target(con)` — mirror export.py's COPY idiom: `config.GOLD_DIR.mkdir(parents=True, exist_ok=True)`, then for path `p = (config.GOLD_DIR / "dim_class_target").as_posix()` run `COPY (SELECT * FROM dim_class_target) TO '{p}.parquet' (FORMAT PARQUET)` and `COPY (SELECT * FROM dim_class_target) TO '{p}.csv' (FORMAT CSV, HEADER)`. Add the same docstring provenance note: targets are audit-cited (AG 2019.AU2.2 / May 2023 FSD report), never recalculated; only config values reach the SQL string.

    Do NOT add `"dim_class_target"` to `config.GOLD_TABLES` — that list drives `GOLD_EXPECTED_ROWS` and the 5-table `export.write_gold` contract guarded by existing tests; keep this reference dimension on its own dedicated builder/exporter so existing tests stay green.

    Generate the committed files by running the builder+exporter once (see Verify).
  </action>
  <verify>
    <automated>cd "C:\Users\leeaa\Toronto-anchored-City-of-Toronto-Fleet-Ferry-Performance-Dashboard"; uv run python -c "import duckdb; from fleet_analytics import class_target; c=duckdb.connect(':memory:'); class_target.build_class_target(c); class_target.write_class_target(c); n,nl,nt=c.execute('SELECT COUNT(*), COUNT(DISTINCT class_label), COUNT(DISTINCT target) FROM dim_class_target').fetchone(); assert (n,nl,nt)==(10,5,5), (n,nl,nt); print('OK', n, nl, nt)"</automated>
  </verify>
  <acceptance_criteria>
    - `data/gold/dim_class_target.parquet` and `data/gold/dim_class_target.csv` both exist.
    - The in-DB `dim_class_target` table has 10 rows, 5 distinct class_label, 5 distinct target.
    - `class_target.py` binds the 5 target values via `?` params (no literal 95/92/85/88/90 in any SQL string).
    - The module docstring documents the UNIT_TYPE (not CATEGORY_CLASS) key decision.
    - `"dim_class_target"` is NOT added to `config.GOLD_TABLES`.
  </acceptance_criteria>
  <done>The committed reference dimension exists as Parquet+CSV, keyed on UNIT_TYPE, built only from config constants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pytest guard — exact 5 target values + committed-file roundtrip</name>
  <read_first>
    - tests/test_dimensions.py lines 14-39 (the in-DB row/value guard idiom)
    - tests/test_export.py (the second-:memory:-connection read_parquet roundtrip idiom in try/finally; the `null_ct`-not-`nulls` reserved-word pitfall)
    - tests/conftest.py (the session-scoped `gold` fixture build chain — you will add dim_class_target to it)
    - src/fleet_analytics/class_target.py (the builder you are guarding)
  </read_first>
  <behavior>
    - A parametrized test over the 5 (class_label, target) pairs — Light/95, Medium/92, Heavy/85, Off-Road/88, Other/90 — passes when each label's target matches.
    - A row-count guard asserts the in-DB table has 10 rows and 5 distinct class_label values.
    - A roundtrip test re-reads data/gold/dim_class_target.parquet via a second :memory: connection and confirms target where class_label='Heavy' == 85.
  </behavior>
  <action>
    Wire `dim_class_target` into the build chain: in `tests/conftest.py`, add a single `class_target.build_class_target(con)` line to the session-scoped `gold` fixture AFTER `kpis.build_all(con)` (import `class_target` alongside the existing `kpis, model, transform` import — follow the precedent documented in the fixture docstring for how prior phases added a build line).

    Create `tests/test_class_target.py` mirroring test_dimensions.py:
    (1) `test_class_target_rows(gold)` — assert `SELECT COUNT(*), COUNT(DISTINCT class_label) FROM dim_class_target` returns (10, 5).
    (2) `@pytest.mark.parametrize("class_label, expected", [("Light",95),("Medium",92),("Heavy",85),("Off-Road",88),("Other",90)])` over a `test_class_target_value(gold, class_label, expected)` that asserts `SELECT DISTINCT target FROM dim_class_target WHERE class_label = ?` == expected.
    (3) `test_class_target_parquet_roundtrip(gold)` — call `class_target.write_class_target(gold)`, then open a SECOND `duckdb.connect(":memory:")` inside try/finally, `read_parquet((config.GOLD_DIR / 'dim_class_target.parquet').as_posix())`, assert target for class_label='Heavy' == 85, and `rd.close()` in finally. Use `(config.GOLD_DIR / name).as_posix()` path helpers (never hard-code paths); if you alias a count column, name it `*_ct` (avoid the `nulls`/`both` reserved-word collision noted in test_export.py).

    Confirm the existing suite still passes (the 5-table export contract must remain green since dim_class_target was NOT added to GOLD_TABLES).
  </action>
  <verify>
    <automated>cd "C:\Users\leeaa\Toronto-anchored-City-of-Toronto-Fleet-Ferry-Performance-Dashboard"; uv run pytest tests/test_class_target.py tests/test_export.py tests/test_dimensions.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_class_target.py -q` exits 0 with the 5 parametrized cases + row-count + roundtrip tests.
    - `uv run pytest tests/test_export.py tests/test_dimensions.py -q` still exits 0 (existing 5-table contract unbroken).
    - conftest.py `gold` fixture builds dim_class_target via one added line.
    - The roundtrip test reads the committed Parquet through a second :memory: connection in try/finally.
  </acceptance_criteria>
  <done>The 5 audit targets are locked as a falsifiable pytest guard; the committed Parquet is re-read and confirmed; existing tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config constants → DuckDB SQL string | The only values reaching the COPY/VALUES SQL are internal `config.ASSET_CLASS_TARGETS` / `UNIT_TYPE_TO_CLASS` / `GOLD_DIR`. No network, no user input, no external file content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | dim_class_target target values | mitigate | Values bound via `?` from `config.ASSET_CLASS_TARGETS` (audit-cited, never inlined/recalculated); pytest guard asserts exact 5 values so any drift fails CI. |
| T-04-02 | Information Disclosure | COPY f-string path interpolation | accept | Only `config.GOLD_DIR`-derived internal paths and the literal table name are interpolated — no external/user value reaches the SQL string (same guarantee as export.py). Low/none risk. |
| T-04-SC | Tampering | uv/PyPI installs | accept | No new dependencies introduced this plan; reuses already-pinned duckdb/pandera/pytest. No package-manager install task. |

No high-severity threats: local ETL producing a 5-value table from trusted local config, no untrusted input, no auth surface.
</threat_model>

<verification>
- `uv run pytest tests/test_class_target.py -q` green (5 parametrized + row-count + roundtrip).
- `data/gold/dim_class_target.{parquet,csv}` exist and re-read clean.
- Existing suite unbroken: `uv run pytest tests/test_export.py tests/test_dimensions.py -q` green.
- `class_target.py` join key is UNIT_TYPE, documented in the module docstring.
</verification>

<success_criteria>
- A committed `dim_class_target` reference dimension exists (Parquet primary + CSV secondary), keyed on UNIT_TYPE so it reproduces the validated Phase-3 by-class targets.
- The 5 audit targets (95/92/85/88/90) are locked by a parametrized pytest guard.
- The D-03/D-04 join-key discrepancy is resolved in code (UNIT_TYPE bridge, not CATEGORY_CLASS) — ready for the report spec to reference.
</success_criteria>

<output>
Create `.planning/phases/04-power-bi-report-specification/04-01-SUMMARY.md` when done. Record the chosen grain/key (10-row UNIT_TYPE bridge), why CATEGORY_CLASS was rejected, and the exported file paths — Plans 02/03 reference this relationship.
</output>
