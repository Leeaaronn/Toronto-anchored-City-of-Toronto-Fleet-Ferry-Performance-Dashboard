---
phase: 04-power-bi-report-specification
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - deliverables/report_spec.md
autonomous: true
requirements: [REPORT-01]

must_haves:
  truths:
    - "report_spec.md opens with the deliverable-doc house style and a 'How to read' blockquote, linking the other Phase-3 deliverables"
    - "A Column Reference Reconciliation table corrects the measures_spec.md placeholders that do not match actual Gold column names (dim_time.hour not hour_of_day; dim_date date column = date_key, no bare 'date'; fact_ferry 'Sales Count'/'Redemption Count' with spaces)"
    - "The model-setup section defines single-direction dim->fact relationships, NO fleet<->ferry relationship, Mark-as-Date-Table on dim_date with date column date_key, role-playing owner/using division, and dim_class_target->fact_vehicle on UNIT_TYPE"
    - "A City of Toronto civic theme JSON block (navy ~#003F87) with color-locked status semantics (red=below target, green=at/above) and an accessibility note is present"
    - "A slicer plan documents Division/Asset Class/Year synced across all pages + Ferry Season/Daypart, with cross-highlight default"
    - "The Fleet Maintenance page is fully specified with per-visual type, real Gold fields, DAX from measures_spec, and SQL validation value"
    - "D-01: City of Toronto civic theme JSON; D-04: mandatory column-reference reconciliation table; D-05: single-direction dim->fact relationships + Mark-as-Date-Table + role-playing division; D-06: synced-core + page-specific slicer plan with cross-highlight default — all delivered in this plan's report_spec.md scaffold"
  artifacts:
    - path: "deliverables/report_spec.md"
      provides: "Report spec scaffold + model setup + theme + reconciliation + slicer plan + Fleet Maintenance page"
      min_lines: 120
      contains: "## Column Reference Reconciliation"
  key_links:
    - from: "deliverables/report_spec.md"
      to: "data/gold/*.csv"
      via: "every referenced column name verbatim from the Gold headers"
      pattern: "date_key"
    - from: "deliverables/report_spec.md"
      to: "data/gold/dim_class_target.parquet"
      via: "dim_class_target -> fact_vehicle relationship on UNIT_TYPE"
      pattern: "dim_class_target"
---

<objective>
Author the first half of `deliverables/report_spec.md` — the model-setup foundation the user wires before placing any visual: the doc header, the mandatory Column Reference Reconciliation table (D-04), the relationships + Mark-as-Date-Table + role-playing setup (D-05), the City of Toronto civic theme JSON (D-01), the synced slicer plan (D-06), and the fully-specified Fleet Maintenance page.

Purpose: This is the contract between the Gold layer and the manually-authored Power BI canvas. It must reference ONLY column/table names confirmed in `data/gold/*` and carry, per visual, the SQL validation value the user's measure must reproduce.
Output: `deliverables/report_spec.md` (scaffold through end of the Fleet Maintenance page). Plan 03 appends the Ferry + Summary pages and PDF layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-power-bi-report-specification/04-CONTEXT.md
@.planning/phases/04-power-bi-report-specification/04-PATTERNS.md
@.planning/phases/04-power-bi-report-specification/04-01-SUMMARY.md
@deliverables/measures_spec.md
@deliverables/kpi_definitions.md
@deliverables/dq_report.md

<interfaces>
<!-- AUTHORITATIVE Gold column headers (verbatim, from data/gold/*.csv read 2026-06-04). -->
<!-- The spec MUST reference ONLY these names. measures_spec.md uses placeholders that do NOT all match. -->

fact_vehicle:  _id, UNIT_NO, YEAR, MAKE, MODEL, CATEGORY, CAT_DESC, UNIT_TYPE, CATEGORY_CLASS, CAT_GRP, IN_SERV_DT, STATUS_DESC, CLASS2, HIGH_PRIORITY, OWNER_DIVISION, REF_DIVISION, SEASONAL, AVAILABILITY_YTD, unit_key_int, fleet_age, Utilization, specialized_units, REF_USING_DIV, owner_division_key, using_division_key
fact_ferry:    _id, Timestamp, Redemption Count, Sales Count, ts_15, season, daypart, day_of_week, is_weekend, sales_redemption_gap
dim_date:      date_key, year, month, month_name, day, day_of_week, is_weekend, quarter
dim_time:      time_key, time_of_day, hour, minute
dim_division:  division_key, division_name
dim_class_target (from Plan 01): unit_type, class_label, target   # 10-row bridge, relates to fact_vehicle[UNIT_TYPE]

<!-- CONFIRMED D-04 fixes the spec MUST apply (do NOT blind-copy measures_spec.md): -->
<!-- 1. dim_time hour column is `hour` (NOT hour_of_day). -->
<!-- 2. dim_date has NO bare `date`. Mark-as-Date-Table column = date_key; all DATEADD/time-intelligence DAX uses dim_date[date_key]. -->
<!-- 3. fact_ferry measure columns are `Sales Count` / `Redemption Count` (embedded space) -> bracket them: fact_ferry[Sales Count]. -->
<!-- 4. fact_vehicle: availability=AVAILABILITY_YTD, utilization=Utilization, specialized=specialized_units, class label via UNIT_TYPE; division FKs owner_division_key / using_division_key. -->
<!-- 5. dim_class_target relates on UNIT_TYPE (Plan 01), NOT CATEGORY_CLASS. measures_spec A2/A3/A5 RELATED(DimClassTarget[Target]) resolves through the UNIT_TYPE relationship. -->
</interfaces>

<deliverable_house_style>
<!-- From measures_spec.md / kpi_definitions.md lines 1-8 — replicate this exact shape. -->
H1: `# Power BI Report Specification — Phase 4 (REPORT-01)`
Then a bold metadata block: **Project:** / **Scope:** / **Method:** / **Snapshot pull date:** **2026-06-02**.
Then a `>` "How to read this spec" blockquote.
Inline relative links to the sibling deliverables: [`measures_spec.md`](measures_spec.md), [`kpi_definitions.md`](kpi_definitions.md), [DQ report](dq_report.md), [`data_dictionary.md`](data_dictionary.md).
Repeat these locked framings verbatim where relevant:
- Availability measures "exclude the 209 NULL `AVAILABILITY_YTD` values (denominator 4,405)."
- Disposal cross-measure is a "screening list for SME review, never a disposal decision."
- Targets are "audit-cited, never recalculated" (AG 2019.AU2.2 / May 2023 FSD General Government Committee report).
- Underutilization "5.8% computed vs ~14% cited" reconciliation (AG 2019.AU2.3).
Per-visual table columns: `Visual | Type | Fields (Gold cols) | DAX measure | SQL validation value | DQ footnote`.
</deliverable_house_style>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold + reconciliation table + model setup + theme + slicer plan</name>
  <read_first>
    - deliverables/measures_spec.md (head lines 1-8 for house style; the full DAX tables to transcribe later)
    - deliverables/kpi_definitions.md (head lines 1-8 for house style; audit benchmarks + AG-theme traceability)
    - deliverables/dq_report.md (head + §2 for the 209-null/4,405-denom + 6-unmatched caveats to footnote)
    - data/gold/dim_date.csv, data/gold/dim_time.csv, data/gold/fact_ferry.csv, data/gold/fact_vehicle.csv (CONFIRM the verbatim headers before writing any column name)
    - .planning/phases/04-power-bi-report-specification/04-01-SUMMARY.md (the dim_class_target grain/key Plan 01 chose)
  </read_first>
  <action>
    Create `deliverables/report_spec.md`. Write, in order:

    (1) HEADER — the house-style H1 `# Power BI Report Specification — Phase 4 (REPORT-01)`, the bold **Project:**/**Scope:**/**Method:**/**Snapshot pull date: 2026-06-02** block, and a `>` "How to read this spec" blockquote that states: grouped page -> visual; every visual carries type, real Gold columns, the DAX measure (transcribed from measures_spec.md, not re-derived), the SQL validation value it must reproduce, and a DQ footnote; the scope split is locked (GSD produces this text spec, no `.pbix`/PBIP/TMDL — the canvas is authored manually). Inline-link the four sibling deliverables.

    (2) `## Column Reference Reconciliation` (D-04, MANDATORY) — a table with columns `measures_spec.md placeholder | Actual Gold column | Table | Correction note`. Include at minimum these confirmed corrections: `hour_of_day` -> `hour` (dim_time); `dim_date[date]` -> `dim_date[date_key]` (no bare date; Mark-as-Date-Table column = date_key); confirm `Sales Count`/`Redemption Count` carry an embedded space and must be bracketed `fact_ferry[Sales Count]`; confirm class targets resolve through `dim_class_target[unit_type] -> fact_vehicle[UNIT_TYPE]` NOT `CATEGORY_CLASS`. State explicitly: "the spec references only names confirmed in data/gold/*; DAX is corrected here, not copy-pasted blind from measures_spec.md."

    (3) `## Model Setup & Relationships` (D-05) — list every relationship single-direction dim->fact: `dim_date[date_key] -> fact_ferry` (slot date derived from ts_15/Timestamp); `dim_time[time_key]/hour -> fact_ferry`; `dim_division -> fact_vehicle` with ROLE-PLAYING owner vs using division — choose and DOCUMENT one approach (recommend: two relationships from a single dim_division to owner_division_key [active] and using_division_key [inactive, activated via USERELATIONSHIP in the using-division measures], OR two dim_division instances; pick one and state the exact USERELATIONSHIP guidance); `dim_class_target[unit_type] -> fact_vehicle[UNIT_TYPE]` single-direction. State the "Mark as Date Table" instruction on dim_date with date column = `date_key`. State explicitly: fact_vehicle is a cross-sectional availability snapshot with no ferry time grain — keep the two fact tables independent, NO fleet<->ferry relationship.

    (4) `## Power BI Theme (City of Toronto civic)` (D-01) — a fenced ```json theme block: a civic navy base (`"#003F87"`) with accessible accent colors and standard fonts, a `dataColors` palette, and good/bad colors locked to status semantics (red = below class target, green = at/above target). Below the JSON, state the color-lock rule (red=below, green=at/above applied to gap-to-target visuals and conditional formatting everywhere) and an accessibility note: avoid red/green-only encoding where feasible — pair color with sign (e.g. "−0.0588") and position (sort worst-first) so the panel reading is not color-dependent.

    (5) `## Slicer Plan` (D-06) — a table `Page | Slicer field (Gold col) | Slicer type | Sync scope`: `Division` (dim_division[division_name]), `Asset Class` (fact_vehicle[CATEGORY_CLASS]), and `Year` (dim_date[year]) SYNCED across all three pages; the Ferry page ADDS `Season` (fact_ferry[season]) and `Daypart` (fact_ferry[daypart]), page-local. State the default visual interaction = cross-highlight (not full cross-filter) so context stays visible during drill, and note slicer type per field (dropdown vs list).
  </action>
  <verify>
    <automated>cd "C:\Users\leeaa\Toronto-anchored-City-of-Toronto-Fleet-Ferry-Performance-Dashboard"; uv run python -c "import re,sys; t=open('deliverables/report_spec.md',encoding='utf-8').read(); body='\n'.join(l for l in t.splitlines() if not l.lstrip().startswith('#')); assert '## Column Reference Reconciliation' in t; assert 'date_key' in t and re.search(r'dim_time\[hour\]|dim_time.*\bhour\b', t); assert 'Mark as Date Table' in t; assert '#003F87' in t; assert 'no `.pbix`' in t.lower() or 'no .pbix' in t.lower() or '.pbix' in t; print('OK reconciliation/theme/model present')"</automated>
  </verify>
  <acceptance_criteria>
    - `deliverables/report_spec.md` contains a `## Column Reference Reconciliation` section.
    - The spec references `date_key` (not bare `date`) for Mark-as-Date-Table and references `dim_time[hour]` (not `hour_of_day`).
    - A theme JSON block containing `#003F87` is present with locked red/below + green/at-above semantics and an accessibility note.
    - Model setup states NO fleet<->ferry relationship and role-playing owner/using division, plus dim_class_target on UNIT_TYPE.
    - Slicer plan marks Division/Asset Class/Year synced all pages + Ferry Season/Daypart, cross-highlight default.
  </acceptance_criteria>
  <done>The report-spec foundation (header, reconciliation, model, theme, slicers) is written against confirmed Gold names.</done>
</task>

<task type="auto">
  <name>Task 2: Fleet Maintenance page — per-visual DAX ⋈ SQL validation value</name>
  <read_first>
    - deliverables/measures_spec.md §"Fleet Maintenance (Domain A)" A1-A5 (transcribe DAX + SQL validation values verbatim, applying D-04 column corrections)
    - deliverables/kpi_definitions.md (audit benchmarks + KPI<->AG-theme traceability AU2.2)
    - deliverables/dq_report.md §2 + §7 (209-null/4,405-denom; 6-unmatched; 2,080 matched — the footnotes)
    - deliverables/report_spec.md (the scaffold from Task 1 — append, do not rewrite)
    - data/kpi/availability_by_class.csv, data/kpi/exception_list.csv (the falsifiable values)
  </read_first>
  <action>
    Append a `## Page 1 — Fleet Maintenance` section to `deliverables/report_spec.md`. Lead with the AG downtime theme (AU2.2). Spec each visual in a per-visual table row `Visual | Type | Fields (Gold cols) | DAX measure | SQL validation value | DQ footnote`, transcribing DAX from measures_spec A1-A5 with D-04 corrections applied (use `fact_vehicle[AVAILABILITY_YTD]`, `fact_vehicle[Utilization]`, `fact_vehicle[specialized_units]`; class target via the `dim_class_target` UNIT_TYPE relationship — `RELATED(dim_class_target[target])`, NOT `DimClassTarget[Target]` placeholder). Cover, per D-07 visual types:

    - A1 Overall Availability Rate (pooled) — KPI card. DAX `Availability Rate (Pooled) = DIVIDE(SUM(fact_vehicle[AVAILABILITY_YTD]), COUNTROWS(FILTER(fact_vehicle, NOT ISBLANK(fact_vehicle[AVAILABILITY_YTD]))))`. SQL validation value **0.8899**. Footnote: excludes 209 NULLs, denominator 4,405. Note: pooled, never average-of-averages (guard value 0.8786 mentioned as the WRONG total).
    - A2 Availability by Class vs Target — clustered bar OR bullet/bar-with-target-line (gap-to-target). Fields: fact_vehicle[UNIT_TYPE] (or class_label via dim_class_target) on axis. DAX for rate-by-class + `Gap to Target = [Availability Rate by Class] - DIVIDE([Class Target],100)`. SQL validation values: Light 0.9149 / Medium 0.8612 / Heavy 0.7948 / Off-Road 0.8882 / Other 0.9337; targets 95/92/85/88/90; signed gaps Light −0.0351 / Medium −0.0588 / Heavy −0.0552 / Off-Road +0.0082 / Other +0.0337. Color-lock red below / green at-above; signed, never abs().
    - A3 Exception List — table/matrix sorted gap_to_target ascending (worst first). `Below Target Unit Count` SQL validation value **1,734**. Footnote: strict `<` excludes NULL availability.
    - A4 Underutilization Rate — KPI card + by-using-division bar. `Underutilization Rate = DIVIDE(CALCULATE(COUNTROWS(fact_vehicle), fact_vehicle[Utilization]="Underutilized"), CALCULATE(COUNTROWS(fact_vehicle), NOT ISBLANK(fact_vehicle[Utilization])))`. SQL validation value **0.0572 ≈ 5.8%**, over the 2,080 matched units. Footnote: 5.8% computed vs ~14% cited (AG 2019.AU2.3) reconciliation; classification pre-applied, never recomputed. Include Specialized Share by division.
    - A5 Disposal-Candidate screening list — table/matrix sorted worst-first. `Disposal Candidate Count` SQL validation value **34**. LABEL the visual "screening list for SME review" and state explicitly: SME screening list, NEVER a disposal decision (the cross-dataset availability⋈utilization value-add). Footnote: 6 unmatched UNIT_NO documented in dq_report.

    End the page with the AG-theme traceability note (these visuals map to AU2.2 downtime + AU2.3 underutilization).
  </action>
  <verify>
    <automated>cd "C:\Users\leeaa\Toronto-anchored-City-of-Toronto-Fleet-Ferry-Performance-Dashboard"; uv run python -c "t=open('deliverables/report_spec.md',encoding='utf-8').read(); [__import__('sys').exit(f'missing {v}') for v in ['Fleet Maintenance','0.8899','1,734','0.0572','34','screening list for SME review'] if v not in t]; assert 'AVAILABILITY_YTD' in t and '4,405' in t and 'disposal decision' in t; print('OK fleet page validation values present')"</automated>
  </verify>
  <acceptance_criteria>
    - `## Page 1 — Fleet Maintenance` section exists with one table row per visual (A1-A5).
    - Every DAX measure block is paired with its SQL validation value (0.8899 / by-class rates / 1,734 / 0.0572 / 34).
    - The disposal visual is labeled "screening list for SME review" and explicitly states "never a disposal decision".
    - DAX uses corrected Gold column names (AVAILABILITY_YTD, Utilization, specialized_units, dim_class_target[target] via UNIT_TYPE) — no placeholder column names.
    - The 209-null/4,405-denominator footnote and the 5.8%-vs-~14% reconciliation appear.
  </acceptance_criteria>
  <done>The Fleet Maintenance page is fully specified, every visual falsifiable against a Phase-3 validation value, with corrected Gold column names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gold CSV headers → spec text | The spec transcribes confirmed Gold column names + Phase-3-committed validation values. No code execution, no network, no user input — a markdown deliverable only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | wrong column names / invented validation values in spec | mitigate | Column Reference Reconciliation table forces every name to a confirmed Gold header; every validation value transcribed from measures_spec.md/data/kpi (not re-estimated); verify step greps the load-bearing values. |
| T-04-04 | Repudiation | uncited audit targets/benchmarks | accept→mitigate | "Audit-cited, never recalculated" framing repeated with AG 2019.AU2.2/AU2.3 + May 2023 FSD citations; Open Government Licence – Toronto. |
| T-04-SC | Tampering | uv/PyPI installs | accept | No dependencies installed (documentation-only plan). No package-manager task. |

No high-severity threats: a markdown spec derived from committed local artifacts; no untrusted input, no auth, no network.
</threat_model>

<verification>
- `## Column Reference Reconciliation` present; spec uses `date_key` and `dim_time[hour]`, not the placeholders.
- Theme JSON with `#003F87` and locked red/green status semantics + accessibility note present.
- Model setup: single-direction relationships, NO fleet<->ferry, role-playing division, dim_class_target on UNIT_TYPE, Mark-as-Date-Table on date_key.
- Slicer plan: Division/Asset Class/Year synced + Ferry Season/Daypart, cross-highlight default.
- Fleet Maintenance page: A1-A5 each carry DAX + SQL validation value (0.8899 / 1,734 / 0.0572 / 34); disposal labeled SME screening list.
</verification>

<success_criteria>
- The report-spec foundation (header, reconciliation, model, theme, slicers) and the Fleet Maintenance page are written against ONLY confirmed Gold names.
- Every Fleet visual is falsifiable against a carried Phase-3 SQL validation value, DIVIDE used for ratios, pooled-mean used for the grand total.
</success_criteria>

<output>
Create `.planning/phases/04-power-bi-report-specification/04-02-SUMMARY.md` when done. Record the role-playing-division approach chosen and any reconciliation corrections applied — Plan 03 continues the same document.
</output>
