---
phase: 04-power-bi-report-specification
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - deliverables/report_spec.md
autonomous: true
requirements: [REPORT-01]

must_haves:
  truths:
    - "The Ferry Operations page is fully specified with per-visual type, real Gold fields (Sales Count/Redemption Count with spaces, day_of_week, dim_time[hour]), DAX, and SQL validation values"
    - "The Summary/Insights page leads AG-themes-first: (1) downtime gap-to-target, (2) underutilization 5.8% vs ~14%, (3) 34-unit disposal screening list (prominent value-add), (4) ferry demand"
    - "The ferry YoY visual shows the 2020<2019 COVID dip and uses dim_date[date_key] for DATEADD (not bare date)"
    - "A PDF-export layout section specifies three 16:9 landscape pages, fit-to-page"
    - "DQ footnotes surface 209 nulls/4,405 denom, 6 unmatched UNIT_NO, and the sales-vs-redemption SME-validation flag"
  artifacts:
    - path: "deliverables/report_spec.md"
      provides: "Ferry page + Summary/Insights page + PDF-export layout + sources/licence — completes the spec"
      contains: "## PDF Export Layout"
  key_links:
    - from: "deliverables/report_spec.md"
      to: "data/gold/fact_ferry.csv"
      via: "fact_ferry[Sales Count] / fact_ferry[Redemption Count] bracketed (embedded space)"
      pattern: "Sales Count"
    - from: "deliverables/report_spec.md"
      to: "data/gold/dim_date.csv"
      via: "DATEADD(dim_date[date_key], -1, YEAR) for ferry YoY"
      pattern: "date_key"
---

<objective>
Complete `deliverables/report_spec.md` — append the Ferry Operations page (Domain B), the AG-themes-first Summary/Insights page (D-02), the PDF-export layout (D-08), and the sources/licence footer. After this plan the spec covers all three pages and satisfies REPORT-01.

Purpose: The Ferry page tells the demand/staffing story (COVID dip + DoW×hour heatmap); the Summary page leads with the two AG themes and keeps the availability⋈utilization disposal screen prominent as the differentiator. The PDF layout makes the 10-minute panel export reproducible.
Output: the completed `deliverables/report_spec.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-power-bi-report-specification/04-CONTEXT.md
@.planning/phases/04-power-bi-report-specification/04-PATTERNS.md
@.planning/phases/04-power-bi-report-specification/04-02-SUMMARY.md
@deliverables/measures_spec.md
@deliverables/kpi_definitions.md
@deliverables/dq_report.md

<interfaces>
<!-- AUTHORITATIVE Gold headers (verbatim) — reference ONLY these. -->
fact_ferry:    _id, Timestamp, Redemption Count, Sales Count, ts_15, season, daypart, day_of_week, is_weekend, sales_redemption_gap
dim_date:      date_key, year, month, month_name, day, day_of_week, is_weekend, quarter
dim_time:      time_key, time_of_day, hour, minute
fact_vehicle:  ... UNIT_TYPE, CATEGORY_CLASS, AVAILABILITY_YTD, Utilization, specialized_units ...
dim_class_target (Plan 01): unit_type, class_label, target   # relates to fact_vehicle[UNIT_TYPE]

<!-- CONFIRMED D-04 fixes that bite on the Ferry page: -->
<!-- ferry measure columns `Sales Count`/`Redemption Count` carry an embedded space -> ALWAYS bracket: fact_ferry[Sales Count]. -->
<!-- ferry YoY DATEADD uses dim_date[date_key] (NO bare `date`). -->
<!-- heatmap columns = dim_time[hour] (NOT hour_of_day); rows = fact_ferry[day_of_week]. -->
</interfaces>

<ferry_validation_values>
<!-- Transcribe from measures_spec.md §"Ferry Operations (Domain B)" B1-B6 + data/kpi — do NOT re-estimate. -->
B1 Lifetime Sales 13,257,804 / Lifetime Redemptions 13,076,317  (SUM fact_ferry[Sales Count]/[Redemption Count])
B2 YoY: 2019 sales 1,249,725 / 2020 sales 366,606; 2020 YoY −0.7067 (COVID dip; complete years 2016-2025 only); DATEADD(dim_date[date_key],-1,YEAR)
B3 Seasonality: pool every year by calendar month (ALLEXCEPT), data/kpi/ferry_seasonality; summer peaks; reuse season field
B4 Heatmap: SUM(fact_ferry[Sales Count]); rows fact_ferry[day_of_week] × cols dim_time[hour]; data/kpi/ferry_heatmap.csv
B5 Sales-Redemption Gap (signed, never abs()): 2016 +92,649 / 2020 −7,940; reuse fact_ferry[sales_redemption_gap]; flagged for SME validation (dq_report A3)
B6 Distribution: Sales Max 7,229 / Sales Median 12 (heavy right skew, not a data error)
</ferry_validation_values>

<summary_narrative>
<!-- D-02 AG-themes-first hierarchy, in this exact order: -->
1. DOWNTIME — availability gap-to-target by class (Light/Medium/Heavy below benchmark; Off-Road/Other clear it).
2. UNDERUTILIZATION — 5.8% current vs ~14% audit (Mar 2023), with the reconciliation note (AG 2019.AU2.3).
3. 34-UNIT DISPOSAL SCREENING LIST — the availability⋈utilization cross-dataset value-add, kept VISUALLY PROMINENT as the differentiator; framed explicitly as an SME screening list, NOT a disposal decision.
4. FERRY DEMAND — COVID dip + DoW×hour staffing, last.
</summary_narrative>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ferry Operations page — per-visual DAX ⋈ SQL validation value</name>
  <read_first>
    - deliverables/measures_spec.md §"Ferry Operations (Domain B)" B1-B6 (transcribe DAX + validation values, apply D-04: bracket `Sales Count`/`Redemption Count`, DATEADD on dim_date[date_key], heatmap cols dim_time[hour])
    - deliverables/dq_report.md §"ferry"/distribution + A3 (sales-vs-redemption SME flag; median 12 / max 7,229)
    - deliverables/report_spec.md (the in-progress doc — APPEND the Ferry page after the Fleet Maintenance page; do not rewrite earlier sections)
    - data/gold/fact_ferry.csv + data/gold/dim_time.csv (CONFIRM `Sales Count`/`Redemption Count` spaces and `dim_time[hour]`)
  </read_first>
  <action>
    Append `## Page 2 — Ferry Operations` to `deliverables/report_spec.md`. Per-visual table rows `Visual | Type | Fields (Gold cols) | DAX measure | SQL validation value | DQ footnote`, transcribing B1-B6 from measures_spec with D-04 corrections (ALWAYS bracket `fact_ferry[Sales Count]` / `fact_ferry[Redemption Count]`; DATEADD uses `dim_date[date_key]`; heatmap columns = `dim_time[hour]`). Cover per D-07 visual types:

    - B1 Lifetime totals — KPI cards. `Lifetime Sales = SUM(fact_ferry[Sales Count])` -> **13,257,804**; `Lifetime Redemptions = SUM(fact_ferry[Redemption Count])` -> **13,076,317**. Footnote: all data, full span May 2015 -> Jun 2026.
    - B2 YoY trend — line chart with a 2020-dip callout. `Sales by Year` restricted to complete years 2016-2025 (dim_date[year] >= 2016 && <= 2025); `Sales YoY Growth = DIVIDE([Sales by Year] - CALCULATE([Sales by Year], DATEADD(dim_date[date_key], -1, YEAR)), CALCULATE([Sales by Year], DATEADD(dim_date[date_key], -1, YEAR)))`. SQL validation: 2019 1,249,725 / 2020 366,606 / 2020 YoY −0.7067. Footnote: 2020<2019 COVID dip is a locked guard; partial 2015/2026 excluded from YoY only.
    - B3 Seasonality — column/line. `Sales by Month = CALCULATE(SUM(fact_ferry[Sales Count]), ALLEXCEPT(dim_date, dim_date[month]))`. Validation: profile in data/kpi/ferry_seasonality; summer peaks; no annualization/partial-year estimation.
    - B4 DoW×Hour heatmap — matrix with conditional-formatting color scale. `Sales by DoW x Hour = SUM(fact_ferry[Sales Count])`; rows fact_ferry[day_of_week] × cols dim_time[hour]. Validation: data/kpi/ferry_heatmap.csv.
    - B5 Sales-Redemption gap — SIGNED bar (never abs()). `Sales-Redemption Gap = SUM(fact_ferry[Sales Count]) - SUM(fact_ferry[Redemption Count])`. Validation: 2016 +92,649 / 2020 −7,940. Footnote: sales-vs-redemption interpretation FLAGGED for SME validation (dq_report A3).
    - B6 Distribution stats — KPI cards. `Sales Max = MAX(fact_ferry[Sales Count])` -> **7,229**; `Sales Median = MEDIAN(fact_ferry[Sales Count])` -> **12**. Footnote: heavy right skew driven by real peak windows, not a data error.

    Optionally (planner discretion, D-deferred): an avg-daily-volume-by-season or peak-day callout MAY be folded in if it strengthens the staffing story — not required.
  </action>
  <verify>
    <automated>cd "C:\Users\leeaa\Toronto-anchored-City-of-Toronto-Fleet-Ferry-Performance-Dashboard"; uv run python -c "t=open('deliverables/report_spec.md',encoding='utf-8').read(); [__import__('sys').exit(f'missing {v}') for v in ['Ferry Operations','13,257,804','13,076,317','7,229','fact_ferry[Sales Count]','date_key'] if v not in t]; assert '12' in t and 'SME validation' in t; assert 'hour_of_day' not in t; print('OK ferry page present, no hour_of_day placeholder')"</automated>
  </verify>
  <acceptance_criteria>
    - `## Page 2 — Ferry Operations` exists with B1-B6 each carrying DAX + SQL validation value (13,257,804 / 13,076,317 / 7,229 / 12).
    - All ferry measure DAX brackets `fact_ferry[Sales Count]` / `fact_ferry[Redemption Count]` (embedded space).
    - YoY DAX uses `DATEADD(dim_date[date_key], ...)` and the page notes the 2020<2019 COVID dip.
    - The heatmap uses `dim_time[hour]` (the string `hour_of_day` must NOT appear anywhere in the doc).
    - The sales-redemption gap is signed and flagged for SME validation.
  </acceptance_criteria>
  <done>The Ferry Operations page is fully specified with corrected Gold names and carried validation values.</done>
</task>

<task type="auto">
  <name>Task 2: Summary/Insights page (AG-themes-first) + PDF layout + sources</name>
  <read_first>
    - deliverables/kpi_definitions.md (KPI<->AG-theme traceability AU2.2/AU2.3; the 5.8% vs ~14% reconciliation)
    - deliverables/report_spec.md (append the Summary page after the Ferry page; reuse measures already specified — reference them, do not re-derive)
    - .planning/phases/04-power-bi-report-specification/04-CONTEXT.md D-02 (the exact AG-themes-first ordering) + D-08 (PDF layout)
    - deliverables/dq_report.md (the three DQ footnotes to consolidate)
  </read_first>
  <action>
    Append three closing sections to `deliverables/report_spec.md`:

    (1) `## Page 3 — Summary / Insights` — AG-themes-first hierarchy in this EXACT order (D-02): (1) DOWNTIME — availability gap-to-target by class (Light/Medium/Heavy below benchmark; Off-Road/Other clear it), reusing the A2 gap-to-target measure + signed values; (2) UNDERUTILIZATION — 5.8% computed vs ~14% audit cited (Mar 2023), WITH the reconciliation note (AG 2019.AU2.3); (3) the 34-UNIT DISPOSAL SCREENING LIST — the availability⋈utilization cross-dataset value-add, kept VISUALLY PROMINENT as the differentiator, framed explicitly as a "screening list for SME review, never a disposal decision"; (4) FERRY DEMAND — COVID dip + DoW×hour staffing, last. Use KPI cards / a compact gap-to-target bar / the screening-list table; state which measures each visual reuses (no new DAX needed — these are summary tiles over the Page-1/Page-2 measures). Keep status color-locked (red below / green at-above).

    (2) `## DQ Footnotes (consolidated)` — a short list surfacing the locked caveats as the footnotes/tooltips used across pages: 209 excluded NULL availabilities (denominator 4,405); 6 unmatched UNIT_NO (availability⋈utilization, dq_report §7); sales-vs-redemption interpretation flagged for SME validation (dq_report A3). State these are audit-defensible stated assumptions.

    (3) `## PDF Export Layout` (D-08) — three 16:9 landscape pages (one per report page: Fleet Maintenance, Ferry Operations, Summary/Insights), fit-to-page; instruct setting the page size to 16:9 in Power BI BEFORE export (File -> Export -> PDF). A one-page exec summary is optional, not required. Then a `## Sources & Licence` footer: City of Toronto Open Data portal; May 2023 FSD General Government Committee report; Auditor General Operational Review 2019.AU2.2 / 2019.AU2.3; Open Government Licence – Toronto. End with the standard closing note: DAX is a text build contract — no `.pbix`/PBIP/TMDL is generated; the canvas is authored manually; audit thresholds cited, never recalculated.
  </action>
  <verify>
    <automated>cd "C:\Users\leeaa\Toronto-anchored-City-of-Toronto-Fleet-Ferry-Performance-Dashboard"; uv run python -c "t=open('deliverables/report_spec.md',encoding='utf-8').read(); [__import__('sys').exit(f'missing {v}') for v in ['Summary / Insights','## PDF Export Layout','16:9','Open Government Licence','6 unmatched','4,405'] if v not in t]; assert 'screening list for SME review' in t and 'never a disposal decision' in t; assert '2019.AU2.2' in t and '2019.AU2.3' in t; import re; assert re.search(r'Page 1', t) and re.search(r'Page 2', t) and re.search(r'Page 3', t); print('OK summary+pdf+sources, all 3 pages present')"</automated>
  </verify>
  <acceptance_criteria>
    - `## Page 3 — Summary / Insights` orders the narrative AG-themes-first: downtime, then underutilization (5.8% vs ~14%), then the 34-unit disposal screening list (prominent), then ferry demand.
    - The disposal screen is labeled "screening list for SME review" and "never a disposal decision".
    - `## DQ Footnotes` surfaces 209 nulls/4,405 denom, 6 unmatched UNIT_NO, and the sales-vs-redemption SME flag.
    - `## PDF Export Layout` specifies three 16:9 landscape pages, fit-to-page.
    - `## Sources & Licence` cites Open Data, May 2023 FSD report, AG 2019.AU2.2/AU2.3, Open Government Licence – Toronto.
    - All three `Page 1/2/3` sections are present in the completed doc.
  </acceptance_criteria>
  <done>The report spec is complete — three pages, AG-themes-first summary, consolidated DQ footnotes, PDF layout, and sources. REPORT-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gold headers + Phase-3 committed values → spec text | Markdown deliverable transcribing confirmed names + committed validation values. No code, no network, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | wrong ferry column names (missing the embedded space) / invented validation values | mitigate | DAX brackets `fact_ferry[Sales Count]`/`[Redemption Count]` per the reconciliation table; values transcribed from measures_spec/data/kpi; verify greps the load-bearing values and asserts `hour_of_day` is absent. |
| T-04-06 | Repudiation | disposal screen mis-framed as a decision; uncited audit numbers | mitigate | "Screening list for SME review, never a disposal decision" + AG 2019.AU2.2/AU2.3 + Open Government Licence citations asserted by the verify step. |
| T-04-SC | Tampering | uv/PyPI installs | accept | No dependencies installed (documentation-only plan). No package-manager task. |

No high-severity threats: a markdown spec derived from committed local artifacts; no untrusted input, no auth, no network.
</threat_model>

<verification>
- Ferry page: B1-B6 each carry DAX + SQL validation value; `fact_ferry[Sales Count]` bracketed; DATEADD on `dim_date[date_key]`; heatmap on `dim_time[hour]`; `hour_of_day` absent doc-wide.
- Summary page: AG-themes-first order (downtime -> underutilization -> 34-unit disposal screen -> ferry); disposal labeled SME screening list, never a decision.
- DQ footnotes consolidated (209/4,405, 6 unmatched, sales-vs-redemption SME flag).
- PDF layout: three 16:9 landscape pages, fit-to-page.
- Sources & Licence footer with the three primary sources + Open Government Licence – Toronto.
- All three Page 1/2/3 sections present.
</verification>

<success_criteria>
- All three pages (Fleet Maintenance, Ferry Operations, Summary/Insights) are specified against only confirmed Gold names.
- Every visual is falsifiable against a carried Phase-3 SQL validation value; DIVIDE for ratios; pooled-mean for grand totals; signed gaps never abs().
- The spec includes the slicer plan, theme, visual types per page, and a PDF-export layout — REPORT-01 fully satisfied.
</success_criteria>

<output>
Create `.planning/phases/04-power-bi-report-specification/04-03-SUMMARY.md` when done. Confirm the completed report_spec.md covers all three pages + PDF layout and note any deferred ferry visuals folded in.
</output>
